# アプリのレート制限(HTTP 429)に何度も当たるIP=上限を無視して叩き続けているクライアント
# (2026-06〜09の実ログでは「TLM-Audit-Scanner」等のスキャナーが10分に100〜336回)。
[INCLUDES]
before = eigo-common.conf

[Definition]
failregex = ^\{.*"remote_ip":"<HOST>",.*"status":429,
ignoreregex = %(_ignore_bot)s
