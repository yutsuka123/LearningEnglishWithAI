# /login への短時間の大量GET(2026-09-15に10分で356件・非ブラウザUAを観測。9/23にも1IPが大量アクセス)。
# パスワード送信を伴わないためアプリ側のブルートフォース対策(user×IP等)の対象外だった穴を補う。
[INCLUDES]
before = eigo-common.conf

[Definition]
failregex = ^\{.*"remote_ip":"<HOST>",.*"method":"GET","host":"[^"]*","uri":"/login(?:[/?"])
ignoreregex = %(_ignore_bot)s
