# CHANGELOG

バージョンを上げるたびに、ここに概要と対応する git コミットを記録する。
**バージョンアップは必ず git commit + push とセットで行うこと**
（`app/config.py`の`APP_VERSION`を上げたら、このファイルに追記してから
コミットする。詳細な作業ログは非公開の`docs/TODO.md`/`docs/TODO_OLD.md`
を参照）。**2026-08-19〜: 基本デプロイのたびに末尾の桁を+1する運用に変更**
（例: 1.1.0→1.1.1）。ユーザーから別途指示があった場合のみ上位の桁を
上げる。

## ver1.2.43 (2026-09-04 03:00デプロイ予定・2026-09-03作業・commit d69d154)

**🎮 ゲーム機能第一弾「クロスワード」を追加(テストユーザー+管理者限定公開・テスト中表示あり)**

- 左ペインに「🎮 ゲーム」タブを新設(管理者+テスト許可リストのみ表示、
  一般ユーザーには機能自体が見えない・`app/services/games_access.py`、
  PayPayチャージ機能と同方式の限定公開)。ハブ→設定→プレイの3画面、
  設定画面に「テスト中」バナー表示。
- 分野(複数選択可)または単語帳を単語ソースに選び、6〜20語(既定10)の
  クロスワードを生成。`app/services/crossword_gen.py`(貪欲法+複数
  試行、平均交差2箇所以上・最低1箇所を狙う配置選択、外部依存なし。
  斜め配置も一度試作したが「ややこしくなる」との判断で撤回)。
- クリューモードを4種から選択可能: 日本語訳モード・英英モード・
  音声モード(発音再生だけ常時減点なし)・ヒント制(何も無料表示しない)。
  日本語訳・英英モードはさらにスタイルを選べる:
  - 日本語: 訳語のみ/説明文(AI生成)/ハイブリッド(クリューごとに
    穴埋め文・説明をランダムに混在させる、本物のクロスワードと同じ
    構成)。
  - 英語: 文章の穴埋め(例文マスク)/語の説明(AI生成)/ハイブリッド
    (同上、クリューごとにランダム混在)。
- **ヒント文はAIが単語ごとに1回だけ生成しキャッシュ**
  (`_ensure_english_ai_hints`/`_ensure_japanese_ai_hints`)。既存の
  類義語/説明文をそのまま流用する初期実装は、多義語で意味がズレたり
  (例: gravity(物理)にseriousness)、専門語の説明文が単語自体の綴り・
  カタカナ読みを含んでしまう(例: phononの説明に「phononとは」)問題が
  あったため、「単語自身を含まない」ルールでAIに生成させ、
  `words.detail`にキャッシュして次回以降は無料で再利用する方式に変更。
- ヒント6種(発音/先頭文字/末尾文字/日本語訳/英語ヒント/全オープン)、
  使用すると基礎点の10%ずつ減点。
- **難易度連動スコア**: 正解時の基礎点はTOEICレベル(words.level)の
  1/10。TOEIC範囲超(990+)は100点固定、範囲未満(300-)は10点固定、
  レベル未設定でも最低10点を保証。
- **部分正解の開示+当てずっぽう対策**: 位置一致率80%以上で一致箇所を
  全開示、先頭文字だけ合っていればそこだけ開示。同一クリューで5回
  不正解が続くと0点でギブアップ扱いにし答えを開示(当てずっぽう連打
  による攻略を防止)。
- グリッドのマス目クリックでもクリューを選択可能(交差点はヨコ/タテを
  トグル)。
- レイアウト刷新: 回答欄+ヒントボタンをマス目の上に配置(以前はマス目
  の下だったため、選ぶたびに毎回スクロールが必要だった)。マス目+
  クリュー一覧を横並び(PC)/縦積み(スマホ、720px以下でマス目縮小)の
  レスポンシブ表示に対応。
- AI生成に数秒かかるため、スタートボタンはクリック後disabled+
  「生成中」表示にして連打を防止。
- 将来の全ユーザーランキング(他ユーザー名非公開)・自分の過去スコア
  振り返りは、既存の`crossword_sessions`テーブルへの集計だけで実現
  できる設計にしてある(未実装)。無料サンプルの事前生成・AI利用分の
  課金設計の見直しは今後の課題([docs/COST_ESTIMATE.md](docs/COST_ESTIMATE.md)
  §1.5参照)。
- Playwright+curlで実機確認済み(4クリューモード×各スタイル計8通りの
  生成・回答・ヒント・部分正解・ギブアップ・スコア加算・マスクリック
  選択・PC/スマホ幅レイアウト、JSコンソールエラー0件)。

**🔧 上記の追加修正(未デプロイのため同バージョンに追記・2026-09-03続き)**

- **ヒントの答え漏洩バグを修正**: 専門語で、AI生成ヒントが未生成の語が
  従来のexplanation/訳語(japanese)へフォールバックした際、答えの英語
  綴りがそのまま(例: 「lidar は」)、または訳語のカタカナ読みがそのまま
  (例: motor→「モーター」)ヒントに出てしまう事例が複数件報告された。
  `_leaks_answer()`で英語綴り一致・カタカナ中心語一致の両方を検知し、
  漏洩する場合は訳語へフォールバックせず汎用の準備中メッセージに
  変更(安全な訳語フォールバックという設計自体をやめた)。
- **ハイブリッドの見せ方を「クリューごとにどちらか一方」に変更**: 当初は
  1クリュー内に穴埋め文+意味の両方を結合していたが、ユーザー提示の
  実際の新聞クロスワード例(クリューごとに穴埋め派・説明派が混在する)
  を踏まえ、クリューごとにランダムでどちらか一方を採用する方式に変更。
- **交差数を強化**: 目標語数ちょうどでなく最大3倍の候補をgenerate()に
  渡し、試行ごとに異なる語の組み合わせも探索(`target_count`引数)。
  平均交差数は2.0〜2.7程度(語彙構成に依存)。先頭/末尾文字での交差を
  同点時に優先するタイブレークを追加し、先頭・末尾文字での交差率は
  平均約85%(目標50%を上回る)。
- **語数上限を20→50に拡大**、UIも数値入力から「10の位」「1の位」の
  2つのセレクトボックスに変更。語数に応じてグリッド上限も動的に拡大
  (`crossword_gen.grid_size_for`、最大34マス)。
- **密なパズルでのクラッシュを修正**: 語数が多いと、無関係な2語が
  たまたま隣接するマスを持ち、採番ロジックが語の開始マスを見落として
  KeyErrorになる不具合を修正(`_boundary_for`で各語の前後マスを
  一元管理し、新しい語がそこに重ならないよう検証。採番も隣接マスからの
  推測ではなく配置一覧から直接行うよう変更)。
- **AIヒント生成を並列化**: 50語ゲームで最大7バッチが順番待ちになり
  生成に45秒かかっていた問題を、`ThreadPoolExecutor`で並列呼び出しに
  変更し約7秒に短縮。20語までの上限も撤廃(パズルに実際採用された語
  すべてにヒントを用意)。
- **DB書き込みロック対策**: AI呼び出しがDBトランザクションを開いたまま
  行われるため、他のリクエストと重なると「database is locked」で
  失敗することがあった。`PRAGMA busy_timeout`を設定し、一時的な競合を
  自動リトライで吸収するようにした(`app/database.py`、アプリ全体に
  適用される改善)。
- 希望語数より少なく配置された場合、「分野を増やす/単語帳を変える」
  等のアドバイスをレスポンスの`notice`フィールドで返し、プレイ画面に
  表示するようにした。
- 全クリュー一括ギブアップボタン「🔓 全部答えを見る」を追加
  (`POST /crossword/{id}/giveup-all`)。テスト中の確認作業を効率化。
- 「答えを見る」ヒントのアイコンをリアルな目(👁️)から解錠マーク(🔓)に、
  日本語ヒントの国旗アイコンを削除するなど、細かなUI微調整。
- 設定画面のクリューモード/日本語・英語スタイルのラジオボタン群が
  ひと続きに見えていたのを、枠+見出し付きのグループに変更。「有料/
  無料」という表記が実際の課金と誤解されるとの指摘を受け「減点あり/
  減点なし」表記に統一。
- プレイ画面から直接「🔄 新しいクロスワードを作る」ボタンで設定画面に
  戻れるようにし、直近3件の出題ソース選択をブラウザに記憶して再選択
  しやすくした。

## ver1.2.42 (2026-09-03予定・2026-09-02作業)

**🔒 PayPayテスト確認チェックリストを設定画面(許可リスト対象者も見える場所)に追加**

- `admin_paypay_test.html`のチェックリストは管理者専用URL配下のため、
  設定画面の「①PayPayで購入」を使える許可リスト対象者
  (`ytsuka-biz1@nyangailab.com`等)からは見えなかった(ユーザー指摘
  「テストユーザーにはわかるように」)。`static/js/views.js`の
  チャージカードにも同内容のチェックリストを追加し、実際に購入ボタンが
  ある場所で確認できるようにした。
- `POST /api/paypay-test/selfcheck`の権限を管理者専用から、管理者+許可
  リスト対象者(`paypay.is_test_allowed`)に緩和(`_require_admin_or_
  test_allowed`新設)。実際のPayPay APIには一切触れない安全な検証のみ
  のため許可リスト対象者にも開放。
- Playwrightで許可リストアカウント(role=user)からチェックリスト表示・
  自動チェック実行(4/4成功)を確認済み。

## ver1.2.41 (2026-09-02)

**🔒 PayPay返金時のpt残高取り消しを0円クランプ方式に変更(現時点でも非公開)**

- `reverse_credit_if_refunded`(ver1.2.40で新設)が、既にpt残高が使い
  切られていた場合にマイナス(負債)を許容していた設計をユーザー指示
  「0でクランプかな」に基づき変更。現在の残高を超えて取り消さないよう
  クランプし、クランプが発生した場合は`balance_ledger`のnoteに明記の
  上、警告ログも出力する。
- ⚠️悪用対策(同一ユーザーが購入→即消費→返金を繰り返して無限に無償
  利用するパターン)は未実装・継続検討事項(ユーザー指示「悪用されない
  ように引き続き要検討」)。当面はログでの検知に留める。

## ver1.2.40 (2026-09-02)

**🔒 PayPay実課金導線の重大な安全性の穴を修正(claude-fable-5の第三者レビュー指摘・現時点でも非公開)**

- **サンドボックス決済でも実際のpt付与が起きてしまう抜け穴を修正**
  (最重要)。`app/services/paypay.py`の`credit_if_completed`が
  `is_production()`を確認せずpt付与していたため、admin/許可リスト
  アカウントがサンドボックス決済(実入金0円)をしても実際のptが付与
  されてしまう状態だった。付与直前に本番モード確認を追加、非本番時は
  警告ログを出して付与をスキップするよう修正。
- **返金してもpt残高が戻らない問題を修正**。`paypay_payments`に
  `refunded_at`列を新設し、`reverse_credit_if_refunded`関数
  (`credit_if_completed`と対の原子的処理)を追加。管理者テストページの
  返金操作(`/api/paypay-test/refund`)から、実課金導線経由の支払いなら
  自動でpt残高も取り消すよう連携。
- **本番VPSに`reconcile_paypay_payments.py`のcronが未登録だった問題を
  修正**(15分おき登録、動作確認済み)。ブラウザに戻ってこなかった
  支払いの取りこぼし救済が実際に機能するようになった。
- 管理者用PayPayテストページ(`admin_paypay_test.html`)に、PayPay公式
  「本番にむけたチェックリスト」+本アプリ固有の確認項目+イレギュラー
  操作の確認項目をまとめた「④本番稼働前チェックリスト」を新設。安全に
  自動実行できる項目(金額検証・他ユーザーアクセス拒否・レート制限・
  公開フラグ)は「自動チェックを実行」ボタンでその場検証でき、結果は
  正常/異常とも`paypay_actions`に記録される。

## ver1.2.39 (2026-09-02)

**🔒 設定画面の「チャージ」に新PayPay即時購入導線を追加(現時点では管理者+限定テストアカウントのみ・一般公開はまだ)**

- PayPay加盟店審査完了(ただし本番API利用開始まで最大5営業日待ちとの通知
  あり)を受け、`static/js/views.js`の設定画面`#chargeCard`を「①PayPayで
  購入(即時反映)」「②BASEで購入してキーを入力」の2ブロックに再構成。
  ①は`POST /api/paypay/create`（2026-09-01実装済みだったが未接続だった
  フロント導線）を呼び出し、支払い完了で即座にpt残高へ反映される
  (キー発行不要)。②は既存のBASE経由フローをそのまま維持。
- **アクセス制御**: role='admin'に加えて、`app/services/paypay.py`に
  新設した`is_test_allowed()`(コード直書きの少人数向け許可リスト)で
  指定した特定アカウントにも限定的に開放。バックエンド
  (`app/routers/paypay_charge.py`の`_guard_not_yet_public`)・フロント
  (`static/js/app.js`の`state.canTestPaypayCharge`、`/api/system/my-usage`
  の`paypay_charge_test_allowed`フィールド)の両方で同じ判定を使用。
- Playwrightで管理者・許可リスト対象・一般ユーザーの3パターンを検証
  (前2者は表示・APIアクセス可、一般ユーザーは非表示・API 403を確認)。
  実際の購入ボタンクリックでは本番API側が現時点で認証エラーを返す
  (加盟店審査は完了したが本番API有効化がまだ・想定内)ことも確認、
  エラー表示は正常にハンドリングされコンソールエラー無し。

## ver1.2.38 (2026-09-02)

**🐛 英単語・フレーズ検索ボックスが連続入力すると反映が遅れ「固まって見える」不具合を修正**

- ユーザー報告「英単語検索で一回検索すると、次検索する際再読み込みしてから
  でないと検索できない」に対応。`static/js/views.js`の`vocab()`/
  `phrases()`双方で、キーワード検索欄(`#kw`)の`input`イベントが毎回
  `/api/words`・`/api/phrases`へ**全件を再取得**していたのが原因と判明
  (キーワード自体はサーバのクエリパラメータに含まれず、絞り込みは
  取得後にクライアント側で行っていたため、再取得は本来不要だった)。
  すばやく連続入力すると同一の重いリクエストが何本も並行発行され、
  ブラウザの同時接続数制限で滞留し、表示の更新が数秒以上遅れて
  「検索してももう一度入力し直しても反映されない」ように見えていた
  (Playwrightで実際のキー入力を再現し、キーワードだけの変更で毎回
  ネットワーク往復が発生していることを確認して特定)。
- 直近のサーバ取得結果をキャッシュ(`baseWords`/`baseList`)し、キーワード
  欄の変更時はキャッシュをその場でクライアント側フィルタするだけに変更
  (`applyKeyword()`新設)。分野・レベル・並び替え等サーバ側フィルタの
  変更時のみ引き続き`/api/words`等を再取得し、取得後に現在のキーワードを
  再適用する。ネットワーク往復が無くなり体感は瞬時に。Playwrightで
  単語・フレーズとも単独/組み合わせ(サーバ側フィルタ変更後もキーワードが
  正しく再適用されるか含む)を確認済み、コンソールエラー無し。

**🔒 PayPay実課金導線の公開前残作業のうち「レート制限」を実装(現時点でも非公開・利用者向けの変更なし)**

- `POST /api/paypay/create`(支払いコード発行)がユーザー単位で無制限に
  呼べてしまう状態だったのを修正。`app/services/paypay.py`に
  `create_rate_limited`を新設し、ユーザーごと直近1時間に10回を超えたら
  エラーコード3003(既存の「試行が多すぎます」)で拒否するようにした。
  既存の`app/services/charge_keys.py`の`redeem_locked`(総当たり対策)や
  `app/services/auth.py`の`ip_rate_limited`と同じ「メモリ内・ユーザー/IP
  単位・時間窓」方式を踏襲。
- この導線自体は引き続き2段階ゲート(`PAYPAY_PRODUCTION_MODE`+
  `app_state.paypay_charge_public_enabled`)で管理者以外は一切利用不可の
  ため、一般ユーザーへの影響は無い。`docs/TODO.md`の「PayPay公開前に
  必要な残作業」5項目のうち1点目に対応(残りはcron登録・app_state有効化・
  UI追加・返金アラート、いずれも本番審査完了後に対応予定)。

## ver1.2.37 (2026-09-02予定・2026-09-01作業)

**🆕 登録導線のUX改善(claude-fable-5監査+レビュー済み・2026-09-02 03:00デプロイ予定)**

- claude-fable-5による視覚+コード監査(スクリーンショット照合込み)で見つかった
  問題点に対応。
- `static/index.html`: 右上「ログイン/登録」ボタンが`display:none`初期値
  で、`/api/system/my-usage`が失敗すると出現条件を満たせず**ゲスト訪問者が
  登録導線を完全に失う**バグを修正。既定を表示にし、ログイン確認できた
  ときだけ隠す方式に反転(`static/js/app.js`の`state.multiuser`/
  `state.isGuest`も同じ理由で既定`true`に変更、失敗時はサイドバーの
  🔑ログイン/登録リンクも出るようにした)。
- `static/login.html`登録フォーム: 「メールアドレスとパスワードだけで
  登録できます」という訴求文言と裏腹に実際は5項目必須だった不整合を
  解消。メールアドレス（確認）欄を廃止(セキュリティ上の意味が薄い割に
  入力の手間が増えるだけと判断)、残る必須項目に「（必須）」を明記。
  パスワード欄とBASE商標の長い注意書きが送信ボタン直前にあり離脱要因に
  なりうる問題は、注意書きを1文に凝縮して送信ボタンの下へ移動。任意の
  アンケート欄の枠線・見出し文字を必須項目より目立たない配色に変更
  (以前は送信ボタンより視覚的に強い枠線で「必須に見える」懸念があった)。
  スマホ幅(390px)で登録ボタンが1画面目に収まることを確認済み。
- claude-fable-5による最終レビューで「デプロイ可」と判定(重大な指摘なし)。

## ver1.2.36 (2026-09-01)

**🐛 「登録者一覧」「アンケート集計」がSQLエラーで動作しなかった不具合を修正**

- ver1.2.35で新設した`GET /api/system/admin/registrants`・
  `GET /api/system/admin/survey-summary`が、リリース直後にユーザー
  実機で試したところ`sqlite3.OperationalError: no such column: u.role`
  で500エラーになっていることが判明。`_user_filter_sql()`が生成する
  絞り込み条件は`u.role`のようにテーブルエイリアス`u`を前提にしている
  のに、両エンドポイントのSQLが`FROM users WHERE ...`とエイリアスを
  付けずに書いていたのが原因(`FROM users u WHERE ...`に修正)。
  ローカルDBで実際にクエリが通ることを確認済み。

## ver1.2.35 (2026-09-01)

**🆕 管理画面に「登録者一覧」「アンケート集計」を追加**

- ユーザー要望「アンケート結果など収集できているか確認」「管理者画面の
  ログにアンケートの確認(内容詳しく集計表示)・登録者の確認を実装」に
  対応。実機テスト登録でアンケート回答が正しくDBに記録されていることを
  確認済み。
- `GET /api/system/admin/registrants`: 登録者一覧(表示名/メール/登録
  日時/アンケート全項目)を新しい順で返す新エンドポイント。既存の
  `/admin/overview`は利用量・課金状態が主目的でメール本体やアンケート
  内容を含んでいなかったため分離。
- `GET /api/system/admin/survey-summary`: アンケートの項目別集計。
  単一選択欄(職業分類/年代/性別)はそのまま、複数選択欄(職業詳細/
  利用目的/きっかけ/興味分野、`', '`区切りで保存)は個別の選択肢に
  分解して集計。自由記述の回答一覧も表示。
- 管理画面「📜 ログ」タブに「🧑‍🎓 登録者一覧（アンケート込み）」
  「📋 アンケート集計」を新設(既存の「🚪 登録に至らない原因分析」の
  直後)。他のログ項目と同じく開いたときに初めてデータ取得する遅延
  読み込み方式。

**🔒 PayPay実課金導線を実装(現時点では非公開・claude-opus-5安全性照査済み)**

- ユーザー指示「実装してください。なお本番環境はまだ審査中です」
  「PayPay支払いはまだユーザーにみせません」に対応。新規:
  `app/routers/paypay_charge.py`(pt購入API)・`templates/paypay_charge.html`
  (支払い後確認ページ)・`scripts/reconcile_paypay_payments.py`(取り
  こぼし救済cron)・DB新設`paypay_payments`テーブル。
- 二重付与防止は`app/services/charge_keys.py`の`redeem_key`と同じ
  「未処理の行だけを対象にした原子的UPDATE」パターンを
  `app/services/paypay.credit_if_completed`に一本化して採用。
- **2段階の公開ゲート**により、`PAYPAY_PRODUCTION_MODE=true`(加盟店
  審査完了後)かつ`app_state.paypay_charge_public_enabled='true'`の
  両方が揃うまで、管理者以外はこの導線を一切利用できない(公開
  リポジトリのためUIを隠すだけでは不十分と判断し、バックエンド側で
  構造的にブロック)。現状はいずれも未達のため完全に非公開。
- claude-opus-5による安全性レビューでクリティカルな指摘なしを確認。
  軽微な指摘(reconcileスクリプトへの本番判定ガード・失敗時のFAILED
  ステータス化・台帳へのsandbox/production記録・専用エラーコード化)
  は反映済み。公開前の残作業(レート制限・cron登録・公開フラグON・
  UI追加)は`docs/TODO.md`に記録。

## ver1.2.34 (2026-09-01)

**🐛 登録フォームが完全に無反応になりうる重大バグ修正 + 計測追加**

- claude-opus-5による独立監査(「登録押せなかった/押せたが登録されなかった/
  わかりにくかった」の3観点)で、`static/login.html`の**最重要バグ**を発見:
  ページ先頭の同期`<script>`で`localStorage.getItem('theme')`が
  無防備に呼ばれており、iOS Safariの「すべてのCookieをブロック」等の
  プライバシー設定下では`SecurityError`を投げて**その1文で以降の
  スクリプト全体が実行されずに止まる**(新規登録ボタンのクリック
  ハンドラ・送信処理を含む)。この状態では新規登録ボタンを押しても
  文字通り何も起きない。4箇所の`localStorage`呼び出しを全て
  `safeGetItem`/`safeSetItem`ヘルパー経由のtry/catchに変更し解消。
- `static/login.html`に`error-report.js`が読み込まれておらず、この
  ページ単体のフロントエンドJSエラーが一切計測されていなかった
  (index.htmlのみ計測対象だった)ため追加。今後同種の不具合が発生
  すれば`client_errors`で検知できる。
- 副次的な改善: 登録欄のメールアドレス入力を`type="email"`(ブラウザ
  ネイティブ検証がバックエンドより厳しく、一部の携帯キャリアメール
  等を誤って弾く恐れ)から`type="text"`+`inputmode="email"`に変更。
  登録ボタンの二重送信ガード(連打で「成功なのに失敗表示」に見える
  事故を防止)を追加。
- 実際の登録ファネルを本番DBで独立検証した結果、この期間に新規登録
  フォームを開いたのはほぼ運営自身のテストのみで、外部からの実訪問は
  極めて少数と判明。導線のわかりやすさ(ウェルカム画面のCTA配置・
  ログイン優先表示・BASE規約文の位置・任意アンケートの視覚的強調等)
  にも複数の改善余地が見つかっており、次回以降の検討課題として記録
  (`docs/TODO.md`参照)。

**🐛 PayPay決済テストのPC無反応バグ修正 + ログ/エラーハンドリング強化
(未デプロイのため同バージョンに追記)**

- 2026-08-27から続いていたPayPayサンドボックス実機決済エラーが
  PayPay側の一時的な不具合と判明・解消(PayPayサポートより連絡・
  ¥800/¥8000のテスト決済で`status=COMPLETED`を確認)。長期最優先課題を
  クローズ(詳細`docs/TODO.md`)。
- **バグ発見・修正**: `templates/admin_paypay_test.html`の支払いボタンが
  常に`deeplink`(`paypay://...`)を優先していたため、PayPayアプリの
  無いPCブラウザでは反応しなかった(ページ遷移が無音無視される)。
  `navigator.userAgent`でモバイル判定し、PC等では`url`
  (ORDER_QRのQRコード表示ページ、スマホのPayPayアプリで読み取り可能)を
  使うよう修正。
- **監査ログの永続化**: PayPayのcreate/details/cancel/refund操作が
  これまでapp.log(テキストログ)のみに記録され、コンテナ再作成
  (`docker compose up -d --build`)のたびに履歴が失われていた
  (実例: デプロイの合間にテスト決済2件のログが消える寸前だった)。
  新設テーブル`paypay_actions`(`app/database.py`)に全操作(成功/失敗
  問わず)を記録するよう変更、`GET /api/paypay-test/history`と
  管理画面③「最近の実行履歴」で振り返れるようにした。
- **エラーハンドリング強化**: `app/services/paypay.py`でタイムアウト
  (`httpx.TimeoutException`)を他の接続エラーと区別し、「決済自体は
  処理されている可能性があるため残高/注文状況を確認してから再送」と
  いう具体的な案内を返すよう変更。

## ver1.2.33 (2026-09-01)

**🐛 パスワード自動入力バグの再修正(ver1.2.32では未解消だった)**

- ver1.2.32で登録欄のname衝突(`password`共有)は解消したが、ユーザー実機
  (Chrome)で「登録画面でパスワードが最初から入っている」不具合が再発、
  さらに「未登録なのにログイン画面でも何か入っている」ことも判明。原因は
  `static/login.html`にログイン用パスワード欄(1個)と登録用の新パスワード欄
  (2個・`autocomplete="new-password"`)が**同一ページに(非表示でも)同時に
  存在**しており、ブラウザ/パスワード管理ソフトがページ全体を「アカウント
  作成ページ」と誤認し、保存データが無くても生成した強力パスワードを
  ログイン欄にまで自動入力してしまっていたと推定(Firefoxでは再現せず、
  Chrome/Google パスワードマネージャー・Keeper等の自動提案機能が疑わしい)。
- 対応: 非表示側フォームのパスワード欄をJSで`disabled`にして完全に除外
  (`showSignup()`/`showLogin()`で相互に切替)。登録用の2欄は
  `autocomplete="off"`+`data-lpignore`/`data-1p-ignore`も追加し自動提案を
  抑制。**ログイン欄(`autocomplete="current-password"`)は一切変更せず**、
  登録済みユーザーの保存済みパスワード自動入力は従来通り機能する。

## ver1.2.32 (2026-08-31・commit `b846a52`)

**🐛 ログイン/登録導線の分かりにくさ修正 + パスワード自動入力バグ修正**

- **ログイン/登録ボタンの文言改善**: トップバー右上・左サイドバーの
  「ログイン」ボタンが、初めての人には登録もできることが伝わりづらい
  という指摘に対応し「ログイン/登録」に変更。`/login`ページの
  「新規登録（無料）」ボタンも視認性を上げるため文字を大きく・太字に。
- **無料登録の訴求文言を具体化**: 「無料で登録できます」→「メール
  アドレスとパスワードだけで、お気軽に無料登録できます」に変更。
- **🐛 バグ修正: 新規登録画面でパスワードが勝手に入力される事故**:
  ログインフォームのパスワード欄と登録フォームの新パスワード欄が
  同じ`name="password"`を共有していたため、ブラウザの保存済み
  パスワードが新規登録の「新しいパスワード」欄に誤って自動入力される
  ことがあった(実機で再現)。新規登録なのに最初からパスワードが入って
  見えると不安に感じるおそれがあるため、登録用フィールドの`name`を
  `new_password`に分離。
- **メール収集への不安を先回りする文言を追加**: 登録フォームのメール
  アドレス欄・プライバシーポリシーに「登録後、営業・勧誘・宣伝・課金の
  催促等のメールは送らない」旨を明記。
- **🆕 猫好き/犬好き向けに分野を分割**: フレーズ「猫・犬にまつわる英語」
  (156件)を「猫にまつわる英語」(74件)/「犬にまつわる英語」(85件)に分割
  (犬猫両方に言及する3件は両分野に複製)。単語も基本語・主要品種名37件
  (cat/dog/kitten/puppy/主要犬種13種/主要猫種7種)を「動物(身近な動物)」
  から既存の「猫」「犬」分野へ移動。犬派/猫派どちらか一方だけの学習者が
  好きな分野だけ選べるようにするための変更（`app/services/taxonomy.py`）。

## ver1.2.31 (2026-08-30・commit `39e2f89`)

**🆕 サーバー監視のグラフ化+30日保持 / フロントJSエラー収集 / 登録に至らない原因分析(常設)**

- **サーバー状態監視**: CPU/RAM/ディスクの保持期間を7日→30日に拡張し、
  24時間/1週間/1ヶ月を切り替えられる推移グラフと期間内の最大値を
  管理画面に追加(既定は畳んだまま)。
- **フロントエンドJSエラー収集**: これまでブラウザのコンソールにしか
  残らなかった未捕捉のJS例外(`window.onerror`/`unhandledrejection`)を
  サーバーに送信・記録するようにし、管理画面にメッセージ別の件数一覧+
  直近ログのパネルを追加。1画面のバグで大量スパムにならないようIPごとの
  レート制限つき。
- **登録に至らない原因分析(常設)**: 未登録訪問者を1人ずつ区別する匿名
  Cookieを新設し、訪問→「このアプリについて」確認→登録試行→登録完了の
  各段階の人数・離脱率と、離脱ポイント(最後に見ていた画面)を管理画面で
  常時確認できるようにした。個別セッションの行動ログもドリルダウンで
  確認可能(この機能導入以降のデータからのみ正確・導入前は既存のIP単位
  集計を参照)。

## ver1.2.30 (2026-08-30・commit `c8fbe38`)

**🆕 想定会話に話者名読み上げオプション + 🐛 左ペインピン留め修正 + pt残高表示の改善**

- **判定・教材の「想定会話」読み上げに話者名アナウンスを追加**(選択制・
  既定OFF、ユーザー承認済みバックログ対応)。`/api/learn/tts`でその場合成
  する読み上げのため事前生成音声の在庫は不要(チェック有無で読み上げ文字列
  が変わるだけ)。既存の英会話(ライブ)機能には2026-08-27に同種の機能が
  既に実装済みだったが、こちらは別画面(教材生成の想定会話サンプル)向け。
- **ピン留め中にハンバーガーで畳めてしまう不具合を修正**(ユーザー報告
  「ピン有効でハンバーガーメニューボタンを押したら畳まれました」)。
  従来はピンが「自動折りたたみの無効化」だけを意味し手動ハンバーガー
  操作は別扱いだったが、「固定表示」という名前から連想する挙動と
  食い違うため、ピン留め中はハンバーガーでの折りたたみ自体を無効化。
  解除したい場合はピン自体をオフにする一貫した仕様に変更。
- **pt残高表示に「残り」を付けて明確化**:「844pt」→「残り844pt」。
- **pt残高にⓘ説明ヒントを追加**: 未登録・無課金でも常に0pt等の数字が
  表示され何の数字か分かりづらいという指摘に対応。タップ/クリックで
  「未登録は0pt、無料登録で毎日一定量、チャージでさらに増える」旨を
  説明するポップオーバーを表示。

## ver1.2.29 (2026-08-30・commit `1a8e719`)

**🎨 ⓘ説明ヒントアイコンの視認性改善**

- 「薄くて見えづらい」というユーザー指摘を受け、`.info-icon`の配色を
  ハンバーガーボタン(`.nav-toggle`)と同じ「水色の縁取り+薄い水色背景」
  に変更(従来は`var(--muted)`の淡いグレーで枠線も`var(--line)`と
  ほぼ背景に埋もれていた)。サイズも16px→20px・フォント11px→13px(太字)に
  若干拡大。`var(--accent)`ベースのためダーク/ライト両テーマで自動的に
  適切な色になる。

## ver1.2.28 (2026-08-30・commit `b0c8b20`)

**🐛 単語/フレーズ一覧のフィルタ更新レース条件を修正 + 「範囲外」チェックの不具合修正 + ログアウト確認ダイアログ追加**

- **ログアウトに確認ダイアログを追加**: 誤操作防止のため、ログアウト
  ボタンを押した直後に`window.confirm()`で「本当にログアウトしますか？」
  と確認し、キャンセルした場合はログアウトAPIを呼ばず何もしない。
  `window.confirm()`は標準のブラウザAPIでPC/スマホ/タブレットいずれの
  ブラウザでも同様に動作するため追加実装は不要(Playwrightでデスクトップ
  幅・スマホ幅の両方で、キャンセル時にAPI未呼び出し・画面遷移なしを確認)。

- **フィルタ変更時のレースコンディションを修正**: 分野選択などフィルタを
  連続で変更すると、先発の遅い応答が後発の速い応答より後から返って
  上書きしてしまい、選び直したのに古い(フィルタ前の)結果が表示される
  ことがあった(ユーザー報告「分野を選択し直しても、たまにフィルター前の
  単語が並ぶ場合がある」)。`static/js/views.js`のvocab()/phrases()の
  `load()`に連番ガードを追加し、自分より新しい問い合わせが発行済みなら
  古い応答の結果を破棄するようにした。
- **「範囲外」チェックが効かないように見える不具合を修正**
  (ユーザー報告「範囲外のチェックを押しても変わらないように見える。この
  機能有効？」)。原因は2つ:
  1. `app/routers/vocabulary.py`/`phrases.py`の`_word_filter`/
     `_phrase_filter`で、「範囲外」チェックはレベル下限・上限を**同時に
     設定した場合のみ**効果を持つ実装になっており、単独でチェックしても
     何も起きなかった。レベル範囲を絞り込んでいない状態でも常に意味を
     持つよう修正(未チェック時は既定で除外、チェック時は含める)。
  2. 現状データでは「範囲外」レベルの語・フレーズは**全件が禁止用語
     (不適切表現)**(単語90件・フレーズ56件、全件が`禁止用語`分野/シーン)
     であり、2026-08-17の禁止用語漏洩再発防止の仕様により「禁止用語も
     表示」を同時にオンにしない限り表示されない。この既定仕様はセキュリ
     ティ上の理由(範囲外選択だけで意図せず不適切表現が漏れるのを防ぐ)で
     維持しつつ、非管理者には「禁止用語も表示」自体が表示されず
     「範囲外」だけあっても常に無効なままだったため、「範囲外」チェック
     ボックス自体を管理者専用に変更(禁止用語表示チェックと同じ条件)。
     管理者向けの説明文言も、この依存関係が分かるよう明記した。

## ver1.2.27 (2026-08-30・commit `c7e312c`)

**🆕 左ペイン折りたたみ+ピン留め / ⓘ説明ヒント機能 / サンプル導線改善 / 緊急メンテ通知の自動非表示**

- **緊急メンテナンス通知の自動非表示**: 臨時メンテナンス終了から6時間は
  「メンテナンスは終了しました」を表示し続け、経過後に自動的に消えるように
  変更(`app/routers/system.py`の`_maintenance_notice()`に`completed`状態を
  追加)。従来は終了時刻ちょうどに即座に消えていた。
- **左ペイン(サイドバー)の折りたたみ+ピン留め**: PC/タブレット幅
  (761〜1200px)では、初回表示後の2回目以降のページ切替時に自動で
  左ペインを折りたたみ、各機能の表の横幅を確保するようにした
  (`static/js/app.js`)。ハンバーガーボタンで折りたたみ/展開を手動切替でき、
  サイドバー内の📌ピンアイコンで「常に開いたまま」に固定できる(固定状態は
  localStorageに保存)。折りたたみ時はトップバーにアプリ名を表示。
- **ⓘ説明ヒント機能を新設**: 「🔊再生できるものだけ」「範囲外」フィルタ、
  単語/フレーズ一覧の再生アイコン(🆓🔊🔒)凡例に説明アイコンを追加
  (`static/js/app.js`の`infoIcon()`)。PCはマウスホバー、スマホ含む全端末は
  タップ/クリックでポップオーバー表示。「今後表示しない」を選ぶとアイコンを
  恒久的に消せる(ログイン済みは`/api/system/user-settings`のDBに
  `dismissed_hints`として保存、ゲストはlocalStorage)。
- **無課金/未ログイン向けサンプル導線を改善**: 英会話・リーディング・
  ライティング・リスニング画面のご案内バナーから「こちら↓」でサンプルへ
  スクロールする方式(既に画面内にあると反応が無いように見える不具合が
  あった)を廃止し、ゲート中はサンプルカード自体を案内の直後(先頭)に
  差し込むように変更(`sampleGateBanner()`/`placeSampleCard()`)。

## ver1.2.26 (2026-08-29・commit `ac5ac7d`)

**🆕 単語分野「歴史」「文学」を国・時代別に細分化 + 「金融」新設**

- 実ユーザーの単語帳/フレーズ帳が0件(既存はadmin自身の4件のみ)であることを
  確認した上で実施(ユーザー承認済み)。
- 既存「歴史」(71語)→「歴史（一般）」に一本化し、新規8分類を空のまま
  新設: 日本史/中国史/世界史（古代）/世界史（中世）/世界史（近代）/
  米国史/ローマ史（古代・帝国）/英国史。
- 既存「文学」(6語)→「文学（一般）」に一本化し、新規7分類を空のまま
  新設: 文学（日本）/文学（中国）/文学（米国）/文学（英国）/
  文学（ロシア）/文学（欧州）/文学（その他）。
- 「金融」を新設(「社会」大分類配下、既存「経済学」から独立)。
  leverage(id 1099)の分野タグを経済学→金融に付け替え、japanese欄も
  「レバレッジ、借入比率／(動詞)活用する、てこ入れする」へ更新
  (金融の語義が埋もれていたため)。
- `app/services/taxonomy.py`の`WORD_CATEGORIES["人文"]`/`["社会"]`を更新。
  新設分野は該当語が追加されるまで一覧のフィルタ候補には出ない
  (既存の「基礎語彙」等と同じ挙動)。

## ver1.2.25 (2026-08-29・commit `5c0bcac`)

**🐛 AI要約ボタンの無反応バグ修正 + 🎙️操作ボタン(音声コマンド)を廃止 + 無課金者向けサンプル導線を追加**

- **学習履歴画面「AIに要約してもらう」ボタンの不具合を修正**: 失敗時に
  「要約中…」の表示のまま固まって見える不具合を修正(エラーハンドリング
  追加・連打防止)。本番ログ分析で、新規登録ユーザーがこのボタンを連打
  した直後にアプリを去り以後未帰還だった実例を確認、離脱原因の一つと
  推定。`app/services/ai.py`の`chat()`にもTTSと同種の生の例外文字列漏洩
  バグがあったため同様に修正。
- **🎙️操作ボタン(音声コマンド機能)を廃止**: 本番利用ログで全期間20
  クリック・実質2ユーザーのみ、しかも大半が数秒間隔の連打(まさに
  abort不具合のパターン)で、機能として使いこなせていなかったため削除
  (関連コード: `app/routers/learn.py`の`/command`エンドポイント、
  `static/js/app.js`の`runCommand`/`execCommand`、
  `static/js/speech.js`の`listenOnce`一式)。
- **英会話/リーディング/ライティング/リスニングに無課金者向けサンプル
  導線を追加**: 無課金・未ログインのユーザーがページ下部の無料サンプル
  に気付けず離脱していた恐れがあったため、冒頭に赤字の案内バナーを追加
  し、クリックでサンプルへジャンプできるようにした。
- **入力:文字/音声セレクタの表示を整理**: 実際に使う「今日の学習」
  「Quiz」「Writing」の3画面だけに表示するよう変更(それ以外の画面では
  意味を持たないため非表示)。
- **単語/フレーズ一覧の検索ボックスをやや縮小**(200px/180px→140px)。

## ver1.2.24 (2026-08-29・commit `6537f3d`)

**🐛 TTS失敗時の生エラー漏洩を修正 + 実機フィードバックによるUI改善3件**

- **TTSの生エラー漏洩を修正**: 音声が未生成(未キャッシュ)のフレーズ/
  単語でTTS合成に失敗すると、OpenAI側の生の例外メッセージ(レート制限の
  JSON等)がそのままトースト表示されていた不具合を修正。
  `app/services/ai.py`の`synthesize_speech()`が例外発生時に
  `f"音声合成に失敗しました: {exc}"`とexcを直接文字列化してユーザーへ
  返していたのが原因(試用してくれた知人の実機フィードバック「フレーズ
  で触るとエラーの生コードが出た」で発覚)。4桁エラーコード体系
  (`app/services/errors.py`)の定型文(8001)を返すよう修正し、生の例外は
  ログのみに留めた。
- **ハンバーガーメニューの視認性改善**: ダークモードで背景に埋もれて
  見えにくいという指摘を受け、`.nav-toggle`に水色(`--accent`)の
  縁取り+薄い背景を追加(既存のテーマ変数を使うためライトモードも
  自動追従)。
- **英単語/フレーズ一覧のフィルターをスマホで折りたたみ式に**: スマホ
  縦画面でフィルターUIが画面の大半を占めていた問題を解消
  (`static/js/views.js`)。狭い画面(760px以下)では既定で折りたたみ、
  それより広い画面(PC/iPad横)では既定で展開。
- **iPad横画面で操作ボタンがカード背景からはみ出る不具合を修正**:
  `.card`に`overflow-x: auto`を追加し、あふれた場合はカード内で
  横スクロールするように変更(添付画面での実機フィードバックで発覚)。

## ver1.2.23 (2026-08-27・commit `474f718`)

**🐛 PayPayテストのredirectUrlがhttp://になる不具合を修正 + 分野「宇宙」新設**

- **PayPayテストのredirectUrl修正**: 本番はCaddy経由でDockerネットワーク
  内部通信のため、FastAPIの`request.base_url`が常に`http://`になって
  しまい（`app/services/auth.py`の`real_client_ip`と同じ既知の落とし穴）、
  `app/routers/paypay_test.py`が生成する`redirectUrl`がPayPayに
  `http://study.nyangailab.com/...`という平文HTTPのまま送られていた
  （実機テストでPayPayアプリ側「エラーが発生しました」の一因と推定）。
  `app/routers/auth_routes.py`の`_cookie_secure`が既に使っていた
  「`X-Forwarded-Proto`ヘッダーから実スキームを判定する」パターンを
  `app/services/auth.py`の共通関数`external_scheme()`として切り出し、
  `paypay_test.py`・`auth_routes.py`の両方から使うように統一。
  ローカルでモックリクエストによる動作確認済み。本番デプロイ後、
  `redirectUrl`が`https://`になっていることをログで確認済み。
- **単語/フレーズ分野「宇宙」を新設**（ユーザー指摘: 「人工衛星」が
  工学・デバイス機器に重複所属、「天文」が理学に埋もれているのは
  違和感がある）: `app/services/taxonomy.py`の`WORD_CATEGORIES`/
  `PHRASE_CATEGORIES`で、天文(天文学の英語)+人工衛星(人工衛星を語る
  英語)を新設「宇宙」大分類に集約。理学/工学/デバイス・機器からは
  該当分野を削除（重複解消）。DBの`words.domain`/`phrases.scene`
  自体は無変更のため既存ユーザーの単語帳への影響なし。
  「インバウンド」(産業/交通への重複所属)はユーザー判断で現状維持。
- **単語分野「基礎語彙」を新設し、domain空欄793件を解消**: available/
  convenient/maintain等、分野機能導入前からある特定分野に属さない
  汎用語彙(id 33〜3687)に新設分野「基礎語彙」を一本化(大分類は既存
  「語学・試験」に合流・ユーザー承認済み)。本番/ローカルDB両方に
  適用済み(本番は事前バックアップ
  `backup_vocabulary_pre_domain_fix_20260827_005744.db`取得後に
  UPDATE)。
- **管理者専用のトップバー「💰 今日 ¥X / 今月 ¥Y」表示を廃止**
  （ユーザー指摘: ver1.2.22で管理画面のユーザー別使用状況テーブルは
  ¥→pt統一したが、こちらの管理者個人向けバッジは対象外のまま残って
  いた）。`static/index.html`の`#costBadge`要素・`static/js/app.js`の
  更新処理・`static/css/style.css`の関連セレクタを削除。管理者も
  他ユーザーと同じ残量表示(`#usageBalance`のpt表記)のみになる。

## ver1.2.22 (2026-08-26・commit `64e518a`)

**🆕 管理画面のユーザー別使用状況テーブルを¥からptに統一 + 残高調整欄の紛らわしい初期値を撤去**

- 管理画面「ユーザー別使用状況」テーブルの残高・日次/月次上限・当日/
  当月利用額の表示を、一般ユーザー向け画面で既に使われている「pt」表記
  (1pt=1円・`app/services/ai.py`の`balance_jpy`と同一単位)に統一
  （ユーザー指摘: 実際には何も自動付与されていないのに、無関係な手動
  残高調整欄の初期値と紛らわしく見えたため）。
- 残高の手動調整欄(`.chg-amt`)に入っていた`value="1000"`という初期値を
  撤去（プレースホルダー「pt」のみに変更）。管理者が金額を入力せずに
  誤って「適用」を押しても金額未入力エラーになり、意図せぬ課金調整が
  起きないようにした。

## ver1.2.21 (2026-08-26・commit `c783270`)

**🆕 日次AI無料枠を100pt/1000pt(admin)のキリのよい値に統一 + 管理画面「上限」表示の誤解を修正**

- **日次AI無料枠を100pt/1000ptに統一**（ユーザー指示）: 個別上限未設定
  ユーザーの既定無料枠を、旧ユーザー(email未設定)向け¥150/日固定から、
  role別に**100pt/日(通常)・1000pt/日(admin)**へ変更
  (`app/services/ai.py`の`_default_daily_cap_usd`にrole分岐を新設、
  `app/config.py`に`DEFAULT_AI_DAILY_FREE_JPY_ADMIN`を追加)。
  従来からある個別`daily_cost_cap_usd`上書き(オーナー約¥1,550/日・
  一部旧ユーザー約¥500/日)は`scripts/reset_daily_cap_overrides.py`で
  一括クリアし新デフォルトへ統一する(月次上限`monthly_cost_cap_usd`は
  意図的に対象外・アルファ配布した友人の月¥500上限はそのまま維持)。
  ローカルDBでは適用・検証済み、**本番DBへの適用は次回メンテナンス
  (2026-08-27 3時)で実施**（DBの直接更新を含むため）。
- **管理画面「上限」表示の誤解を修正**（ユーザー指摘）: 全ユーザー一覧の
  「今日/上限」列が、個別上限未設定のユーザー全員に対して**サイト全体の
  日次上限**(`.env`の`AI_DAILY_COST_CAP_USD`、現在¥50,000相当)をそのまま
  フォールバック表示しており、「これは全員共通の個人上限」に見えて
  誤解を招いていた。実際の利用制御(`ai._user_guard`)と同じ
  `_effective_caps()`を使うよう`app/routers/system.py`の
  `admin_overview`を修正し、role/emailに応じた実効値(100pt/1000pt/
  ¥0等)が正しく表示されるようにした。

## ver1.2.20 (2026-08-26・commit `3f48672`)

**🆕 訪問者数推移グラフに累積表示を追加 + バージョン情報の表示ズレを修正**

- 管理画面「📊 訪問者数の推移」に「累積(延べ)」チェックボックス(既定ON)
  を追加。ONの間はグラフ・テーブルの延べ数(human_total/bot_total)を
  日次の単純合計から累積和に変換して表示する(`static/js/views.js`の
  `toCumulativeVisits()`)。ユーザー要望「延べ累積がほしい・累積が
  デフォで、累積なしはチェックで切替」を反映。ユニークIP数列は日を
  またぐ重複排除ができない(バックエンドがIP集合ではなく件数のみ返す)
  ため対象外とし、これまで通り日次の値を表示する。
- **バージョン情報ページの見出し(`APP_VERSION`)と本文
  (`release_notes.json`)が食い違っていた不具合を修正**（ユーザー
  指摘）。`release_notes.json`は`ver1.2.15`のまま4バージョン
  (ver1.2.16〜1.2.19)分の追記が漏れており、`_readme`内の「CHANGELOG.md
  とセットで追記」という自己注記だけでは防げなかった。CHANGELOG.mdの
  該当4エントリから要約して追記し、CLAUDE.mdの「バージョン管理・
  リリースノート」ルールにも`release_notes.json`更新を明記の手順として
  追加（再発防止）。
- 管理者ダッシュボードの日次AI利用上限(`daily_cap_jpy`)について
  ユーザーから「これは全体の上限では」との確認があり、コードを調査
  （`app/services/ai.py`の`_effective_caps`/`.env`の
  `AI_DAILY_COST_CAP_USD`）して回答: 個別設定
  (`users.daily_cost_cap_usd`)が無いユーザーの行は、サイト全体の
  日次上限(現在322.58 USD≈¥50,000)がそのままフォールバック表示
  されているだけで、ユーザーごとの実質的な利用制御は前払い残高
  (`balance_jpy`)側で行う設計になっている（ユーザーの理解と一致・
  コード変更なし）。

## ver1.2.19 (2026-08-25・commit `461c6fe`)

**🆕 エラーコード体系の全展開完了 + BASEフルフィルメントPhase C(未配送メール通知)を新設**

- **4桁エラーコード体系: 残り82箇所を全ファイルへ展開**（2026-08-20の
  auth_routes.py実装以来止まっていた残タスク）。`base_oauth.py`・
  `billing.py`・`categories.py`・`decks.py`・`fulfillment.py`・
  `inquiries.py`・`learn.py`・`phrase_decks.py`・`phrases.py`・
  `system.py`・`vocabulary.py`の生`raise HTTPException(...)`を全て
  `errors.http_error(code, message)`に置換（文言は既存のものを維持し
  つつコードのみ付与）。新規コード7件を`errors.py`に追加（3011〜3016:
  注文重複/キャンセル済み発行不可/キー発行済み/BASE連携失敗/BASE通信
  失敗/無料範囲上限、7004: 学習記録がある削除保護）。既存コードと文言が
  一致するものは再利用（2004管理者専用・7001見つかりません・7002入力
  不正・3005/3006金額変更等）。置換後、未使用になった`HTTPException`
  importを全ファイルから削除し、`app/`配下の生HTTPExceptionは0件に。
  `scripts/export_error_codes.py`で`docs/ERROR_CODES.md`/`.csv`再生成。
  TestClientで動作確認済み（削除済みフレーズID指定で404+
  `X-Error-Code: 7001`を確認）。
- **BASEフルフィルメントPhase C(未配送メール通知)を新設**: 新規
  `app/services/mailer.py`（Gmail SMTP経由・`GMAIL_ADDRESS`/
  `GMAIL_APP_PASSWORD`未設定時は送信をスキップするのみで同期処理自体は
  継続、送信失敗も同期を止めないbest-effort設計）。
  `app/routers/fulfillment.py`の`sync_orders_from_base()`（Phase B・
  2026-08-19実装済みで本番cronにより30分おきに自動稼働中）に、新規注文
  検知時の即時メール通知と、`OVERDUE_HOURS`(24h)超過の未配送が残る場合の
  リマインド（再送間隔12h・`app_state`でスロットリング）を追加。
  未設定時は通知がスキップされるだけで既存のPhase B自動同期には影響しない
  ことをTestClient/独立した一時DBで確認済み。`.env.example`・
  `deploy/.env.study.example`に設定キー名を追記（実値は書かない）。
- 調査の過程で、BASE Phase B（BASE API自動検知）が2026-08-19に実装済み・
  本番で既に自動稼働中（VPS cron 30分おき）であることが判明。
  `docs/TODO.md`の「Phase B未着手」という記載は単なる更新漏れだったため
  修正した。
- `docs/TODO.md`を整理（削除はせず、完了済みセッションログ約900+216行を
  `docs/TODO_OLD.md`へ移動・散在していた未完了項目を集約）。

## ver1.2.18 (2026-08-24・commit `da15997`)

**🆕 管理画面に「訪問者数の推移」グラフを新設 + ようこそ画面の分野一覧をタップ操作に対応**

- 管理画面「ログ」タブに新セクション「📊 訪問者数の推移（人間/クローラー別）」
  を追加（ユーザー要望「昔の訪問者カウンターに相当するものが欲しい・
  1週間/1ヶ月/3ヶ月/6ヶ月の推移を表とグラフで見たい」）。
  - 新規`GET /api/system/admin/visit-trend`（`app/routers/system.py`）:
    `landing_visits`(kind='visit')を日次集計し、人間/クローラー別に
    延べ数・ユニークIP数を返す。判定は既存の「未登録アクセス状況」と
    同じ`visitor_kind.classify()`を再利用し基準を統一（管理者自身は
    除外）。usage_events(API呼び出しも含む全リクエスト)ではなく
    landing_visits(ページを開くたび1回)を採用したのは、「訪問」の実感に
    近い数字にするため。
  - フロントは期間プリセット(1週間/1ヶ月/3ヶ月/6ヶ月)ボタン・サマリー
    カード4枚・折れ線グラフ(SVG)・日別テーブルの組み合わせ。グラフは
    dataviz skillの手順に従い、`scripts/validate_palette.js`で検証済みの
    2系列カテゴリカルパレット(人間=青/クローラー=橙、両モードで
    CVD安全性チェックPASS)を採用。マーク仕様(2px線・4px半径ドット+
    2pxリング・ホバー時のクロスヘッダ+ツールチップ)も同skillに準拠。
- 「ようこそ」画面の「収録語彙分野一覧」「収録機能一覧」を、横スクロール
  (スワイプ)の帯から、折り返し+縦スクロールの小さな枠(高さ約3行ぶん)に
  変更（ユーザー実機検証で発覚: Android実機で横スワイプが機能せず内容が
  見られなかった。縦スクロールは誰でも迷わない操作方法のため）。
  ラベル文言も太字・白文字（dark modeでの視認性向上要望）に変更。
- ローカルでPlaywright実機確認済み（管理画面: 期間切替・ホバーツール
  チップ・ライト/ダークテーマ双方でコンソールエラー0を確認。ようこそ
  画面: タッチスクロールでのbbox確認、3画面幅で崩れなし）。

## ver1.2.17 (2026-08-24・commit `52bcbde`)

**🆕 新規登録の摩擦をさらに削減 + ようこそ画面の拡充 + 離脱分析用ログ強化**

- **フリガナを任意化**（ユーザー承認）: 「呼んでほしいお名前」は宛名として
  必要なため必須のまま、フリガナだけ必須→任意に変更（打ち間違えても
  ロックされない安全な削減策として選択。メール/パスワードの確認欄は
  パスワード再発行機能が未実装のため据え置き）。`app/routers/auth_routes.py`・
  `errors.py`(コード2013の文言更新)・`static/login.html`を修正。
- **ゲスト向け学習中ナッジ**（ユーザー承認）: ゲストのまま単語/フレーズ系
  タブで5回操作すると、1セッション1回だけ「学習の記録は保存されていません。
  ログインで失われなくなります」というトーストを表示(`static/js/app.js`の
  `initClickTracking()`を拡張)。about.htmlの既存文言だけでは気づかれにくい
  ため、実際に使っている最中に出す方が効果的という判断。
- **「ようこそ」画面の拡充**:
  - ジャンル具体例(物理・天文・法律・医療・料理・アニメ・「失礼にならない
    言い方」・名言 など、すべてtaxonomy.pyに実在する分野)を追記し、抽象的
    だった「ニッチな語彙」訴求を具体化。
  - 「カード不要・お名前はニックネームでOK」を追加し、個人情報の少なさを
    事前に伝える(ログイン画面側の案内文にも同内容を追記)。
  - 主CTA(「30秒で無料登録」)がバッジ(ピル)や副ボタンに埋もれて見える
    との指摘のため、影付き・大きめのボタンに強調。リンク先も`/login#signup`
    に変更し登録フォームまでワンクリックに短縮。
  - 画面下部に「収録語彙分野一覧」(177分野・件数付き)「収録機能一覧」を
    横スクロール(スマホはスワイプ)の帯で追加し、場所を取らずに収録量の
    多さを伝えられるようにした。`GET /api/words/facets`のレスポンスに
    `domain_counts`(分野ごとの語数)を追加(既存の`domains`等は変更なし・
    後方互換)。
  - 実装中、横スクロール帯の中身(177個の分野チップ)がflexboxの既定の
    `min-width:auto`によりカード全体を押し広げてしまうレイアウト崩れを
    発見・`.welcome-card`に`min-width:0`を追加して解消(スマホ実機確認で
    発覚。デスクトップでは症状が出ず見落としやすい典型的な罠)。
- **離脱分析用ログ強化**: これまで`landing_visits`はルート`/`と
  `/static/about.html`しか記録しておらず、「訪問→/login到達→登録フォームを
  開く→送信」のうち「/login到達」「フォームを開く」が完全に見えない穴
  だった。`/login`を記録対象パスに追加し、`static/login.html`に軽量な
  トラッキング(`/api/system/track`への直接fetch、ページ表示時・新規登録
  フォームを開いた時の2箇所)を追加した。
- ローカルでcurl実機確認済み(フリガナ空欄での登録成功、facetsの
  domain_counts反映、trackエンドポイント正常応答、テストユーザー削除済み)、
  Playwrightで「ようこそ」画面のPC/スマホ両方をコンソールエラー0・
  レイアウト崩れなしで確認済み。

## ver1.2.16 (2026-08-24・commit `f593e17`)

**🆕 未ログイン訪問者向けの「ようこそ」画面を新設（訪問しても離脱してしまう問題への対応）**

- ルート `/` にゲスト(未ログイン)が来ると、価値説明なしにいきなり「英単語
  (12,000件)」の生のツール画面が表示され、初見の訪問者が何のアプリか
  分からないまま離脱していた可能性が高いと判断（実機での導線確認で発覚）。
  ゲスト専用の「🏠 ようこそ」タブを新設し、未ログイン時の初期表示にした
  （ログイン済みユーザーは従来通りダッシュボードのまま・変更なし）。
- 訴求内容は「サブスクなし・使った分だけ」「専門用語からニッチな語彙まで」
  「語源・豆知識つきの詳しい解説」の3点（`static/js/views.js`の`welcome()`）。
  内容面のレビュー(照査役)と見た目面のレビュー(見た目確認役)を分けて実施し、
  両方の指摘を反映: 見出しが商品名のみで内容が伝わらない点を修正、CTAを
  「30秒で無料登録」に変更、詳しい説明はabout.htmlへ誘導する形に整理。
- 見た目レビューで「トップバーの操作系UI(🎙️操作・入力モード・残高表示等)が
  そのまま表示され、特にスマホでは設定行が3段続き訴求が画面外に追いやられる」
  との指摘（"致命的"）を受け、`welcome`タブ表示中のみトップバーの当該項目を
  非表示にするCSS/JSを追加（`body.tab-welcome`クラス切替、ブランド・テーマ
  切替・文字サイズ・ログイン導線は残置）。PC版はカード下の空白が目立って
  いた点も、画面の残り高さいっぱいに中央寄せする形で解消。
- ローカルでPlaywright実機確認済み（PC/スマホ双方でコンソールエラー0、
  ようこそ画面がスクロールなしで見出し〜CTAまで収まることを確認）。

**🐛 `/`・`/login`・`/tokushoho` へのHEADリクエストが405を返していた不具合を修正**

- FastAPIの`@app.get(...)`はHEADメソッドを自動サポートしないことが判明
  （Starletteの`Route`はGETと同時登録時にHEADを自動追加するが、実際に
  本番・ローカルの両方で検証したところ405 `Allow: GET`のみが返る挙動を
  確認）。SNSのリンクプレビュー取得や一部の監視ツール・クローラーはHEADを
  先に送るため、リンク共有時にプレビューが正しく取得できない/一部botに
  弾かれている可能性があった。`robots.txt`でAllow指定している主要3ページ
  (`/`・`/login`・`/tokushoho`)を`@app.api_route(..., methods=["GET",
  "HEAD"])`に変更し解消（`/static/*`はStaticFilesが元々HEADに対応済みで
  対象外）。ローカルでcurl・TestClient実機確認済み(HEAD 200)。

## ver1.2.15 (2026-08-23・commit `c0eee83`)

**🆕 新規登録の導線を強化（「ログイン=課金」誤解対策・登録0件の分析対応）**

- 本番エラーログ(data/app.log)を分析。ERROR15件は2026-08-06の一時的な
  DB lock(WAL有効・以後17日再発なし)、WARNING101件はgeoip外部API失敗
  （実害は管理画面の空欄のみ）で、いずれも緊急対応不要と判断。
- 一方、実際の訪問者データ（未登録アクセス状況・端末/ブラウザ列で確認）
  で人間らしいアクセスはあるのに登録0件という状況を確認。`/login`画面が
  「無料」の表示なくいきなりID/パスワード欄から始まる作りで、初見の
  訪問者に有料会員専用ゲートに見えていた可能性が高いと判断し、以下を改善:
  - `static/login.html`: ログインフォームに「初めての方は無料で登録
    できます」＋目立つ「新規登録（無料）」ボタンを追加。新規登録
    フォーム先頭にも「登録は無料です」を明記。任意アンケートを既定で
    折りたたみ、かつ太字＋太い青枠で目立たせて存在に気付きやすくした。
  - `static/about.html`: 3つのチェックリストのうち2・3番目を、機能の
    箇条書きから「ログインしないと記録が消える」（損失回避）
    「実際に話す練習がしたくなったら」（次の一歩を促す）という
    説得力のある文言に変更。
- 管理画面「未登録アクセス状況」に表示件数（明細）セレクタを追加
  （50〜2000件、既定500）。

## ver1.2.14 (2026-08-23・commit `7ee3987`)

**🆕 管理画面「未登録アクセス状況」に端末・ブラウザの推定表示を追加 + about.htmlの法的リンクを目立たせる**

- 新規`app/services/ua_parse.py`を追加。User-Agentから端末
  (iPad/iPhone/Android/Windows/Mac/Linux/ChromeOS)とブラウザ
  (Chrome/Safari/Edge/Firefox/Opera/Samsung Internet)を判定する
  `parse_ua()`/`summarize()`を新設（外部ライブラリ不使用、既存の
  `visitor_kind.py`のボット判定と同じ部分一致方式）。iPadOS Safariが
  既定でMacと同じUAを名乗るため区別できない、という既知の限界も
  コメントで明記。
- `/api/admin/anon-access`(`app/routers/system.py`)のレスポンスに
  `device`/`browser`を追加し、管理画面の「未登録アクセス状況」表
  (`static/js/views.js`)に「端末」「ブラウザ」列を追加。既存の
  国/地域/接続元組織の解析に加えて端末種別も分かるように
  （ユーザー要望: IPからの解析に加えて端末・ブラウザも知りたい）。
- `static/about.html`の利用規約・特定商取引法リンクを、目立たない
  テキストリンクから「アプリを開く」よりやや小さめの明確なボタンに
  変更（読んでもらいやすくする目的・ユーザー要望）。
- ローカルで`ua_parse`の判定ロジックを実データ・代表的なUA文字列で
  動作確認済み（クラッシュなし）。

## ver1.2.13 (2026-08-23・commit `47d6788`)

**🔧 スマホでの見やすさ・分かりやすさの改善（ユーザーのスクリーンショットベースのフィードバック多数対応）**

- 「要ログイン」表示を要ログイン/要課金で出し分け。`state.canPlayOutOfRange`を
  汎用化して`state.hasAiBalance`にリネームし、`views.js`に
  `aiGateLabel()`/`aiGateDisabled()`を新設。英会話(ハンズフリー開始/AIから
  始める)・リーディング/リスニングの生成ボタン・ライティングの送信ボタンに適用。
- メンテナンス予定のお知らせがスマホでメイン画面上部を占領し本文に届きにくい
  との指摘のため、`#maintBanner`をサイドバー(ハンバーガーメニュー)内へ移動。
  未読時はメニューボタンに丸印(has-notice/in-progress)で気付けるようにした。
- 単語クイズ・フレーズクイズを、単語一覧/フレーズ一覧画面から独立した
  「📝 クイズ」メニュータブに分離（一覧画面のスペース確保のため）。
- 単語一覧・フレーズ一覧: 「〜に移動しました」案内文、サブタイトル、別立ての
  「一覧(N)」見出しを削除し、件数はページタイトルに統合
  （例:「ミニフレーズ (6643)」）。
- 単語一覧・フレーズ一覧の「詳細」ボタンと「うろ覚え」ボタンの誤タップ防止の
  ため、間隔を1文字分(1em)広げた(`.ops-cell .vague-btn`)。
- ダッシュボードの統計カード(TOEIC換算・平均習熟度・今日のAI費用、単語の状況)
  をスマホでも3列表示のまま維持（`.grid.stats-wrap`をmedia query内で優先）。
  使われていない「クイックスタート」カードを削除。TOEIC換算に「実際のスコア
  との対応を保証するものではない」旨の注記を追加。
- `static/about.html`のCTAボタン(アプリを開く)直下に利用規約・特定商取引法
  に基づく表記へのリンクを追加(法的表示の視認性向上)。
- ローカルでPlaywright(headless Chromium)によりデスクトップ/モバイル両方の
  ビューポートで実機確認済み（各画面のconsoleエラー無し、テストスクリプトは
  `/tmp`のスクラッチファイルでリポジトリ外）。

## ver1.2.12 (2026-08-22・commit `5154155`)

**🆕 英会話の応答速度を選べるように（ユーザー要望・実測ベンチマークで検証）**

- 英会話画面に「⚡ 応答を高速化（試験運用）」チェックボックスを新設
  （既定OFF＝従来通り）。ONにすると会話専用の高速モデル
  (`OPENAI_CONVERSATION_MODEL`、既定`gpt-4o-mini`)を使う。
- 実測（`scripts/conversation_speed_test.py`新設・2パターン×2回）:
  現行`gpt-5.6-luna`は初回応答まで平均約2.5〜2.9秒。候補4モデルを
  比較した結果、`gpt-4o-mini`が最速(平均約1.1秒・現行の2.2倍速)かつ
  **実費用も現行より安い**（`gpt-4.1-mini`/`gpt-5.4-mini`も高速だが
  `gpt-5.4-mini`は自由会話パターンで学習者の発言をほぼ繰り返すだけの
  機械的な応答が出て品質不安定だったため不採用）。
- pt消費は既存の会話マージン計算（実費用×2.5倍＋0.5円切り上げ）を
  そのまま流用。`gpt-4o-mini`は実費用が現行より低いため、高速化しても
  pt消費が増えることはない（ローカルの`ai_usage`ログで確認済み）。
- `app/services/ai.py`の`chat_stream()`に`model`引数を追加
  （従来は会話モデルを呼び出しごとに差し替えられなかった）。
- ローカルでcurl・実ブラウザ(Playwright)の両方で実機確認済み
  （チェックON/OFFそれぞれで正しいモデルが呼ばれ、`ai_usage`に
  正しい費用で記録されることを確認、テストデータ削除済み）。

## ver1.2.11 (2026-08-22・commit `ce4754e`)

**🆕 新規登録の必須項目を「本名」から「呼んでほしいお名前」へ入れ替え（ユーザー指示）**

- 従来は氏名・フリガナ（本名想定）が必須項目だったが、決済・本人確認には
  使っておらず実質アンケート項目に過ぎなかった。呼びかけ用の名前が要る
  実務上の理由（チャージキー発行等の連絡時の宛名）は本名でなくても
  満たせるため、必須項目を入れ替えた。
  - 新設・**必須**: 「呼んでほしいお名前」(`display_name`、既存列を
    フォーム未接続だったところに接続) と、そのフリガナ
    (`display_name_furigana`、新規列)。仮名・ニックネーム可と明記。
  - 旧来の氏名・フリガナ(`full_name`/`furigana`)は**任意アンケート欄へ
    移動**（ラベルに「（任意）」を明記）。
  - `app/routers/auth_routes.py`のバリデーション対象を差し替え
    （エラーコード2013のメッセージも変更）、`static/login.html`の
    フォーム位置とJS送信ペイロードを更新、`static/privacy.html`
    「取得する情報」の記載も同期。
  - ローカルでcurl実機確認済み（display_name未入力→2013エラー、
    display_name入力+full_name空欄→登録成功・DB保存内容を確認、
    テストデータ削除済み）。

## ver1.2.10 (2026-08-22・commit `3afd4a9`)

**🆕 例文と訳の食い違いを一斉修正 + ネイティブ速度の実測と改善 + バージョン情報ページ/メンテナンス予定/自動デプロイ（ユーザー要望）**

### 1. 詳細画面「例文」と「訳」が別の文になっていた不具合を修正（本番2,452語）

- **報告**: GPIO の詳細で 例文 `We toggled a spare GPIO pin to trigger the
  oscilloscope.` に対し 訳が「このボードのGPIOピンを使ってLEDを制御した。」
  という別の文になっていた。
- **原因**: 例文本体は `words.example`、その訳は `detail(JSON).example_ja` と
  **別々に**保存されている。`scripts/backfill_example_ja.py` は
  **英単語名だけで突き合わせて**訳をマージする作りだったため、あとから
  `words.example` を差し替えても `example_ja` が古いまま残っていた。
- **調査結果**（本番12,085語）: `detail.examples[]` に同じ英文の訳がある
  5,168語のうち **2,463語で食い違い**。うち約2,400件は「まったく別の文の訳」。
- **修正方針**（`scripts/fix_example_ja.py` を新設）: `detail.examples[i]` は
  同じAI生成呼び出しで `en`/`ja` が対で作られるので、`examples[i].en ==
  words.example` の要素の `ja` を正とする。ただし**無条件に置換すると改悪に
  なる**ことを実データで確認したため（`overdue`「期限を過ぎています」→
  「提出期限を過ぎている」のような言い換え、`coalition` は examples 側に
  ペルシャ語 `تشکیل` の混入）、
  - 文字bi-gramのJaccard類似度 **0.55未満**、
  - 訳が2文以上に連結されている、
  - 訳が日本語でない（英文がそのまま入っている・60件）
  のいずれかのときだけ置換し、言い回しの違いだけのものは温存した。
  置換候補に日本語が無い/別言語が混ざる場合は置換しない。
  → **2,452件を置換**（温存9,633件）。ランダム25件を目視で再チェックし、
  すべて妥当であることを確認。実行前に本番DBをバックアップ済み。
- **再発防止（コード修正）**: 置換の有無によらず
  `detail.example_ja_src`（その訳が対応する英文）を全12,085語に記録。
  表示側 `app/routers/vocabulary.py` に `_resolve_example_ja()` を追加し、
  - `example_ja_src` が現在の例文と一致 → そのまま表示
  - 一致しないが `examples[]` に現在の例文があればその訳を使う
  - どちらでもない → **訳を表示しない**（間違った訳を見せない）
  という多重防御にした。詳細の新規生成時・`backfill_example_ja.py` の
  マージ時にも `example_ja_src` を必ず記録するようにした。
- **点検スクリプト** `scripts/audit_translations.py` を新設（読み取り専用）。
  文字化け/略語ズレ/文数の不一致/長さ比の異常を機械的に洗い出す。

### 2. 詳細JSONに混入していた別言語の単語を修正（17語＋フレーズ1件）

- AI生成時にごく低頻度で起きる「日本語の文の途中に外国語の単語が1つ入る」
  現象。例: `coalition`「連立政権を تشکیلした」、`marsh`「густく生えている」、
  `opposition`「反対、 विरोध」。`scripts/fix_garbled_details.py` を新設し、
  **1件ずつ人が確認した置換表**で修正した。
- 語源(origin)欄が正当にアラビア語/ヘブライ語/ハングル/タイ文字を引用して
  いるもの（`Torah` のヘブライ語 תּוֹרָה、`kimchi` の 김치 など80件）は
  **対象外**。一括除去すると教材価値のある語源記述を壊すため。

### 3. ネイティブ速度が「標準」とほぼ変わらなかった件を実測して改善

- `scripts/tts_speed_test.py` を新設し、本番のTTSを実測（12件×5条件）:

  | 条件 | 平均WPM | learn比 |
  |---|---|---|
  | learn（標準・学習用） | 129.7 | 1.00 |
  | native（現行） | 136.6 | **1.07** ← ほぼ差が無い＝指摘は正しい |
  | 指示文を強化した版 | 140.1 | 1.11（効果が小さくばらつく） |
  | 現行指示文＋速度1.15倍 | 151.2 | **1.18**（自然な会話の140〜170に入る）|
  | 現行指示文＋速度1.25倍 | 174.2 | 1.37（速すぎ寄り） |

- 目標WPMを数値で指示する案は、**1語だけの単語で0.3秒＝ほぼ無音**になる
  事故が3件中2件で発生したため不採用（スクリプトに見本として残置）。
- TTS指示文を変えると `(モデル,声,指示文,本文)` のキャッシュが全て無効になり、
  **ストック済みのnative音声37,568件を作り直す費用**が出る。一方、再生速度は
  音程を保ったまま即座に効き費用ゼロ。よって**指示文はそのまま・native選択時の
  再生速度を1.15倍**に変更した。元音声はnative指示文で合成されたリンキング・
  リズムのあるものなので、単なる早送りとは異なる。
- `preservesPitch` を明示（環境によって声が高く/低くなり「早送り」に
  聞こえるのを防ぐ）。
- **英単語一覧・フラッシュ単語・フラッシュフレーズにも「ネイティブ」を追加**。
  従来は「単語にはnative音声が無い」として隠していたが、`audio_store` は
  `word_native` を保存できるので、押されたときに合成して貯まる方式にした。
  （「英単語のネイティブ速度が分からない」という指摘は、そもそも選択肢が
  無かったのが原因だった。）
- 実測に使ったwavは `data/tts_speed_test/` に保存してあり聴き比べ可能。

### 4. 未登録アクセス状況の表示を改善（ユーザー要望）

- **総数を先頭に固定表示**。絞り込み・並べ替え・表示件数に関わらず変わらない
  ように、集計は期間内の**全IP**で行い、表示だけ `limit` で切るよう変更。
  種別ごとに「IP数」と「延べ回数」の両方を出す（管理者自身の大量アクセスの
  ような偏りが見えるように）。
  - 👤 人間のアクセス（管理者を除く・印なし）
  - ※1 管理者自身 ／ 🤖 ※2 機械クローラー ／ 🧠 ※3 AIクローラー ／
    ❓ ※4 その他
- **明細は折りたたみ**にし、種別チェックボックスで絞り込めるようにした
  （**既定は「人間」だけON、機械系はOFF**）。列見出しのクリックで並べ替え
  （IP/場所/組織/初回/最終/回数/説明書閲覧/登録試行）。期間(7/30/90/365日)の
  絞り込みは従来どおり。
- API `GET /api/system/admin/anon-access` に `mark_visits`（印ごとの延べ回数）
  を追加。

### 5. 「バージョン情報」ページを新設

- サイドバーに `🆕 バージョン情報` を追加（未ログインでも閲覧可）。
  最新バージョンと修正内容を先頭に、それ以前は「更新履歴」を開くと表示。
- 掲載元は新設の `release_notes.json`。`public`（一般ユーザー向けの
  当たり障りのない内容）と `admin`（管理者向けの詳細）を分けて持ち、
  管理者の画面では **public を赤文字・admin を通常色**で並べて区別する。
- `data/release_notes.json` があればそちらを優先して読むので、
  **コンテナを作り直さずに（再起動なしで）**掲載内容を差し替えられる。

### 6. メンテナンス予定の表示と自動デプロイ（無停止化の第一歩）

- メンテナンス予定を **DB(`app_state`)** に保持。管理者は「バージョン情報」
  ページから変更でき、**アプリの再起動なしで**利用者側に即反映される。
  - 定期メンテナンス: 既定 **毎週月曜 03:00〜03:30（日本時間）**（曜日・時刻を
    変更可）
  - 臨時メンテナンス: 日時・理由を個別に設定
  - 予定の何時間前からお知らせを出すか（既定24時間前）
- 画面上部に**お知らせバナー**を追加（予定は黄色、作業中は赤・閉じられる）。
- `deploy/scheduled_deploy.sh` を新設（VPSのcronから5分おきに実行）。
  `data/deploy_request.json` に予約があり時刻が来ていたら、
  DBバックアップ → 現行イメージを `eigo-app:prev` に退避 →
  `docker compose up -d --build` → ヘルスチェック（最大60秒）→
  **失敗したら自動で前のイメージにロールバック**、結果を
  `data/deploy_log.jsonl` に記録する。
  管理画面の「この時刻に自動デプロイする」チェックで予約ファイルが作られる。
  **コードは事前に作業機から rsync しておくこと**（VPS上ではgit管理外の
  ため、このスクリプトはコードを取りに行かない）。

### 残課題

- `detail.examples[]` に照合先が無く機械判定できない **6,666語**の例文訳が
  残っている。無作為60件の目視では約12%（推定800件前後）が別の文の訳。
  バッチで順次点検する。

## ver1.2.9 (2026-08-21・commit `39f05e0`)

**🆕 未登録アクセスに訪問者種別の推定（※1〜※4）を追加（ユーザー要望）**

- 管理画面「未登録アクセス状況」の各行に、**User-Agentと接続元組織から
  推定した訪問者種別の印**を付けるようにした。列は増やさず、IPの右に
  上付きの「※1」〜「※4」を出し、意味は表の下の欄外備考で説明する
  （表が横に伸びるのを防ぐためというユーザー指定）。印にマウスを乗せると
  判定理由が出る。
  - ※1 管理者自身（`ADMIN_KNOWN_IPS`）
  - ※2 検索エンジン等のクローラーの可能性（Googlebot/bingbot/Applebot等に
    加え、SNS・チャットのリンクプレビュー(facebookexternalhit・Twitterbot・
    LINE・Chatwork)とSEO巡回もここに含む。「誰かがURLを共有した結果」で
    悪性ではないため）
  - ※3 AI検索クローラーの可能性（GPTBot/ClaudeBot/PerplexityBot/
    meta-externalagent/Google-Extended等）
  - ※4 その他、普通のユーザーではないものの可能性（curl等のHTTP
    クライアント、脆弱性/インフラスキャナ、データセンター発のアクセス）
  - **印なし** … たまたま/興味を持って等を問わず、人間が意識的に閲覧した
    とみなせるもの
- サマリ行に「印なし＝人間とみなせるIPは何件か」を表示。
- 判定ロジックは`app/services/visitor_kind.py`に分離（純粋関数・
  リスト追記だけで運用できる形）。
- **本番実データ125IPでの検証結果**: 印なし7 / ※1 2 / ※2 29 / ※3 7 /
  ※4 80。印なしの7件はすべて日本の一般ISP(IIJ・So-net・KATCH・au)＋
  ポーランド学術網で、目視でも妥当。アクセス回数ベースでは
  279回中106回が管理者自身、人間とみなせるのは8回だった。
- 誤判定を減らすため入れた工夫:
  - 実在ブラウザはほぼ必ずUAに`Mozilla/`を含むため、含まないものは※4
    （UAが`pc`だけ、`RootEvidence/1.0`等が拾える）。
  - 汎用の`bot`判定は単語境界で行う（Android端末名`CUBOT`の誤検出防止）。
  - **Cloudflare/Akamai/Fastlyはデータセンター判定に入れない**。
    iCloudプライベートリレーやCloudflare WARPの出口IPがこれらの事業者名に
    なるため、実在するiPhoneユーザーを※4と誤判定してしまうため。
- **バージョン採番を`RV.L`形式として明文化**（ユーザー指示）。アップの
  たびに末尾`L`を+1し桁上がりしない（`1.2.9`の次は`1.2.10`）。上位2桁は
  ユーザー指示があったときのみ。CLAUDE.mdに追記。

## ver1.2.8 (2026-08-21・commit `6737d7b`)

**🆕 未登録アクセスのgeo情報の取りこぼしを一括補完 + 外部APIのレート制限バグ修正 + 管理者IPを「※1」表示に（ユーザー要望）**

- **バグ修正: 外部geo API(ipapi.co)のレート制限を検知できていなかった。**
  無料枠を超えると **HTTP 429 を `text/plain` で返す**が、旧実装は
  ステータスを見ずに`resp.json()`を呼んでいたため、`error`列には
  `Expecting value: line 1 column 1 (char 0)`という原因の分からない
  例外文字列だけが残り、レート制限だと気づけなかった。HTTPステータスと
  Content-Typeを明示的に判定するよう修正（429は429として記録される）。
- **フォールバック事業者 `ipwho.is` を追加**（HTTPS・サインアップ不要・
  JSON）。第一候補は2026-08-20にユーザーが選択した`ipapi.co`のままで、
  失敗したときだけ次を試す。プライバシーポリシー7項は事業者名を特定
  しない汎用記載のため改定不要。
- **`scripts/backfill_geoip.py`を新設。** geo収集は2026-08-20導入で
  リクエスト時にしか埋まらないため、それ以前に訪問した111IPが管理画面
  「未登録アクセス状況」で国/地域/接続元組織「—」のまま残っていた。
  未取得・取得失敗のIPだけを対象に後追い取得する（既定dry-run・
  `--execute`で実行・連続5件失敗で自動中断）。**本番で102件中101件の
  補完に成功**。残り1件は`172.18.0.x`＝Docker内部ネットワークの
  private IPで、仕様どおりスキップされる（常に「—」で正しい）。
- **管理画面「未登録アクセス状況」で管理者自身のIPを「※1」表示に**
  （ユーザー要望）。従来の👑アイコンを上付きの`※1`に変え、説明は
  表の下の欄外備考に出す（表が横に伸びるのを防ぐため）。対象IPは
  `.env`/`deploy/.env.study`の`ADMIN_KNOWN_IPS`（実IPは個人情報なので
  gitignore対象のファイルにのみ記載・`.env.example`側は説明と
  ドキュメント用IPのみ）。
- **（インフラ・commit `4fd89de`）作業機がmacOS/Windowsの2系統になった**
  ため、OS依存スクリプトを`deploy/macos/`と`deploy/windows/`に分割し、
  `.gitattributes`で全ファイルをLFに固定した。Windowsのgit既定
  `core.autocrlf=true`が`sync_code.sh`をCRLF化して壊していたのと、
  デプロイがgit経由でなく作業ツリーからのrsyncのためCRLFのまま本番へ
  送られる問題への対処。詳細はCLAUDE.md「💻 作業機がmacOS/Windowsの
  2系統になった」を参照。

## ver1.2.7 (2026-08-20・commit `9b4edc2`)

**🆕 未処理例外のログ検知穴を修正 + 管理画面「未登録アクセス状況」+ 共通エラーコード体系(着手)（ユーザー要望）**

- **バグ修正: 想定外の未処理例外(バグ・DBロック等)がdata/app.logに一切
  記録されていなかった**ことが判明(実際に未処理例外を発生させ、
  app.logが空のままになることをテストで確認して発覚)。FastAPI/
  Starletteは既定で500を返すだけでアプリ側ロガーを経由しないため、
  管理画面の「エラーログ」でも検知不能だった。`app/main.py`に
  `@app.exception_handler(Exception)`を追加し、必ず`log.error(...,
  exc_info=exc)`でトレースバックごと記録するよう修正。クライアントへは
  共通エラーコード7999のみ返す。
- ログイン/新規登録は元々正常に動作・エラーも(想定内の失敗は)正しく
  記録されていることをcurl実機で確認済み(今回追加でエラーコード化)。
- **管理画面に「👤 未登録アクセス状況（未ログイン訪問者）」を新設**
  （既定は折りたたみ・ユーザー要望）: トップページ/取扱説明書(about.html)
  の閲覧・新規登録の試行(成否)をIP単位で集計し、国・地域・市区・接続元
  組織名(外部API ipapi.co・HTTPS・無料枠、結果はip_geo_cacheに永続
  キャッシュ)、初回/最終アクセス、アクセス回数、説明書閲覧有無、登録
  試行有無を一覧表示。期間内の未ログイン総アクセス回数・ユニークIP数も
  表示。`landing_visits`テーブルを拡張(kind/success列追加)し、about.html
  閲覧・登録試行も記録するように。IP単位の集計のため、真の「端末単位」
  ではない旨を画面上に明記(端末単位が必要なら別途トラッキングCookie
  導入をユーザーと相談)。
- **全アプリ共通の4桁エラーコード体系に着手**（ユーザー指定の区分:
  1000通信/2000認証/3000課金・チャージ/4000AI/5000ブラウザ/6000OS/
  7000アプリ/8000再生録音/9000その他）。`app/services/errors.py`に
  レジストリを新設し、レスポンスヘッダ`X-Error-Code`で返す設計。一覧は
  `scripts/export_error_codes.py`で`docs/ERROR_CODES.md`/`.csv`に自動
  生成（手編集しない）。今回は認証/登録まわり(auth_routes.py)と
  グローバル例外ハンドラのみ実装、残り約80箇所は次回以降に順次展開。
- ローカルでcurl/Playwright実機確認済み（登録成功/失敗・ログイン成功/
  失敗でのコード付与、未処理例外での7999付与とapp.log記録、管理画面
  新セクションの表示・データ取得、コンソールエラー0）。**本番デプロイ
  済み**（`deploy/sync_code.sh`→dry-runで削除対象なし確認→
  `docker compose -f deploy/docker-compose.study.yml up -d --build`。
  起動ログにエラーなし＝DBマイグレーション(landing_visits拡張・
  ip_geo_cache新設)も正常適用。`/`・`/login`・`/api/health`・
  `/static/about.html`いずれも200、ailab 302で無影響を確認済み）。

## ver1.2.6 (2026-08-20・commit `531b84e`)

**🆕 ログイン履歴・チャージキー入力ログにも集計フィルタを適用（ユーザー要望）**

- `GET /api/system/admin/login-log`・`charge-key-log`に管理者/招待
  ユーザー/テストユーザーの除外フィルタ(ver1.2.5と同じ`include_admin`/
  `include_invited`/`include_test`)を追加。`login_log`はuser_idを持たず
  usernameで記録するため、`users`とusernameでJOINして判定（削除済み
  ユーザーへの言及や存在しないユーザー名への誤ログイン試行はJOINが
  ヒットしないため常に表示対象に含む＝不正アクセス監視を妨げない）。
  `charge-key-log`は従来「管理者のみ固定除外」だったのを、他の集計と
  揃えて切り替え可能にした。
- 画面上部のフィルタチェックボックス3つがこの2つのログにも共通で効く
  ように説明文を更新。
- ローカルでcurl/Playwright実機確認済み（デフォルトフィルタでの絞り
  込み・include_invited=trueでの復元、コンソールエラー0）。

## ver1.2.5 (2026-08-20・commit `f4f77ee`)

**🆕 管理画面の各種集計に「管理者/招待ユーザー/テストユーザー」除外フィルタを追加（ユーザー要望）**

- ユーザー別管理・ユーザー別使用状況・利用状況分析・サーバー状態の
  同時アクセス数、これら全ての集計から既定で(1)管理者自身、
  (2)メール未登録の招待ユーザー(自己サインアップでない従来ユーザー)、
  (3)開発用テストアカウント、の3種類を除外するように変更。管理画面
  上部に3つのチェックボックスを追加し、個別に含める/除外を切り替え
  可能（集計自体は全ユーザー分を保持しており、あくまで表示のみの
  フィルタ）。
- `users`テーブルに`is_test`列を追加。ユーザー別管理テーブルの各行に
  「テスト」チェックボックスを追加し、管理者が手動でマークできる
  （`POST /api/system/admin/users/{id}/test-flag`）。開発用に作成した
  アカウントかどうかは機械的に判定できないため、手動フラグ方式を採用。
- ローカルでcurl/Playwright実機確認済み（デフォルトフィルタでの絞り
  込み・フィルタ切り替え・テストフラグのトグルとDB反映、いずれも
  確認、コンソールエラー0）。

## ver1.2.4 (2026-08-20・commit `33a6843`)

**🆕 サーバー状態監視・ログイン履歴のDNS逆引き表示（ユーザー要望）**

- 管理画面「📜 ログ」に「🖥️ サーバー状態」カードを追加。サクラVPSは
  n8n・ecopy等の他dockerプロジェクトと相乗りのため、コンテナ内からは
  見えないホスト全体のCPU load average・RAM使用率・ディスク使用率を
  表示（上位プランへの乗り換え要否を判断する材料用）。あわせて直近
  5分の同時アクセスユーザー数も表示。新規`scripts/collect_server_stats.py`
  がVPSホスト側cronで5分おきに収集し`data/server_stats.jsonl`へ追記、
  `GET /api/system/admin/server-status`がそれを読むだけ（既存の
  `analyze_access_log.py`と同じ流儀・アプリコンテナ内では計測しない）。
- ログイン履歴（`login_log`）にIPのDNS逆引きホスト名列を追加。
  ログインAPIのレスポンス経路をブロックしないよう、`BackgroundTasks`で
  レスポンス後に解決してDBへ書き戻す設計（`auth.reverse_dns()`/
  `update_login_hostname()`）。動的IP・一般ISPはPTR未設定で解決できない
  ことが多く、その場合は空欄のまま。
- ローカルでcurl/Playwright実機確認済み（マイグレーション適用・新規
  ログインでのホスト名解決・管理画面の表示、いずれも確認）。

## ver1.2.3 (2026-08-19・commit `7487cf4`)

**🆕 チャージキー: 使用済み/無効の表示分離・入力ログの無期限保持、フルフィルメント管理に発行/使用状況と操作履歴を追加**

- `POST /api/billing/redeem`のエラーメッセージを分離。「使用済み/失効
  済み」のキーは「このキーは使用済みです。すでにチャージ済みの可能性
  があります。」と明示するように変更（従来は「存在しない/シークレット
  不一致」と同じ汎用メッセージだった）。使用済み判定はシークレット照合
  より前に行うため、この区別自体は未償還の価値あるキーの総当たり対策
  (error oracle対策)を弱めない。
- 新テーブル`charge_key_attempts`を追加し、キー入力の試行(成功/使用済み
  /無効)を無期限保持でログ化（`usage_events`と違いprune対象外）。
  キー番号そのものは保存せず、secretを含まない一方向ハッシュのみ保持。
  管理画面「📜 ログ」に「🧾 チャージキー入力ログ」を追加。
- `charge_keys.issuance_stats()`(フルフィルメント画面の発行状況サマリ)
  と「🧾 チャージキー入力ログ」から、管理者自身(role=admin)がテストで
  償還したキー/試行を除外するように変更（実際の購入者の動向のみを
  反映するため・2026-08-19ユーザー要望）。
- 新テーブル`base_order_actions`を追加し、BASE注文フルフィルメントの
  操作(追加/自動検知/発行/再発行/配送済み/キャンセル)を「誰が/いつ」
  含めて無期限保持でログ化。フルフィルメント管理画面(`/admin/
  fulfillment`)の各注文行に「履歴」ボタンを追加し、その場で確認できる
  ように。
- 同画面のキー欄に、発行済み/使用済み/失効/未発行の状態バッジと、
  キー番号(公開部分)・発行日時・使用(チャージ)日時を表示するように
  変更。
- 公開サンプル教材(reading/listening/writing/conversation)のTTS音声を
  事前生成する`scripts/build_sample_audio.py`を追加し本番で実行済み
  (40件×2声)。単語/フレーズ全件の音声在庫化(`build_audio.py --all
  --native`)も本番で実行中（1日のコスト上限で自動停止・複数日にわたり
  継続予定）。

## ver1.2.2 (2026-08-19・commit `136a07f`)

**🆕 時間帯別利用状況・ログイン画面に「ログインせず試す」導線を追加**

- `GET /api/system/admin/usage-analytics`に時間帯別(0〜23時・JST・
  対象期間合計)の画面表示/再生/クリック集計を追加。管理画面「📊 利用
  状況分析」に新しい折りたたみ項目として表示（0件の時間帯も0埋めで
  表示・簡易バーグラフ付き）。
- ログイン/新規登録画面(`static/login.html`)に「ログインせずにお試しで
  使ってみる →」リンクを追加（`/`へのリンク。未ログイン訪問者は既存の
  ゲスト疑似ユーザー機構でサンプル閲覧が可能だが、`/login`に来た人が
  それに気づける導線が無かったため）。

## ver1.2.1 (2026-08-19・commit `4c308d0`)

**🆕 利用状況分析に日別新規登録数・流入経路(登録時アンケート)を追加、管理画面に購入キー発行待ち件数を表示**

- `GET /api/system/admin/usage-analytics`の「日別推移」に新規登録数
  (users.created_at集計・ゲスト疑似ユーザーは除外)を追加。従来の
  画面表示/再生/クリックと合わせて1つの表に表示。
- 同APIに「流入経路」集計を追加（サインアップ時アンケート
  survey_referralの複数選択回答を分解して件数化）。管理画面「📊 利用
  状況分析」に新しい折りたたみ項目として表示。
- サインアップ画面の「このアプリを何で知りましたか」の選択肢を
  拡充（Qiita/note/X(旧Twitter)/Instagram/Facebook/その他紹介記事/
  Yahoo/Google/AIに聞いた(OpenAI・Claude・Gemini・Grok)を追加、既存の
  友人から/BASEのページでは維持）。
- 管理者情報画面(👑管理者情報)に「購入キー発行待ち(BASE注文pending)」
  件数を表示（問い合わせ対応と同様、タブ見出しにバッジ表示 +
  購入チャージキー対応タブ内にも件数表示）。BASE注文はVPS側cronで
  30分おきに自動同期済みのため、この画面を開くだけで最新の未配送
  件数が分かる。

## ver1.2.0 (2026-08-19)

**🆕 利用状況分析を項目別に分割 + 単語分野別・フレーズ分野別・年代/性別クロス集計を追加**

- 管理画面「📊 利用状況分析」を、画面別・機能別再生・ボタン押下・
  IP別・単語分野別・フレーズ分野別・日別推移・年代/性別クロス集計の
  各項目ごとに折りたたみ表示へ再構成（データ取得は従来通り1回のAPI
  呼び出しにまとめ、表示だけを分割）。
- `app/services/tracking.py`: `usage_events.kind`に`word_domain`
  （単語の`words.domain`）・`phrase_scene`（フレーズの`phrases.scene`）
  を追加。単語・フレーズの音声再生時(`app/routers/learn.py`の
  `tts_item`)に、再生した項目の分野/シーンを記録するようにした
  （2026-08-19以降の再生から集計対象・過去分は遡って集計できない）。
- `GET /api/system/admin/usage-analytics`に`word_domains`・
  `phrase_scenes`・`demographics`を追加。`demographics`は
  サインアップ時の任意アンケート(`users.survey_age_group`/
  `survey_gender`)と`usage_events.user_id`を突き合わせたクロス集計
  （「40代男性はどの分野をよく使っているか」等を後から分析できる
  ようにする用途。未回答は「(未回答)」にまとめる）。

## ver1.1.9 (2026-08-19)

**🆕 ログ・バックアップの容量方針を確定（100ユーザー・1GB予算）**

- `app.log`: 1MB×3世代(最大4MB)→**10MB×15世代(最大150MB)**に拡張
  （100ユーザー規模で3ヶ月分の突発的なエラー多発にも耐えられるよう）。
- `scripts/prune_usage_events.py`(新設): ボタン押下等の操作ログ
  (`usage_events`)は1ヶ月保持の方針のため、35日より古い行を削除する
  cronスクリプトを追加。VACUUMはDB全体(数GB)をロックする重い操作
  なので意図的に行わない。
- `scripts/backup_user_data.py`(新設): ユーザー登録情報・設定・
  習熟度進捗・単語帳/フレーズ帳を、共有コンテンツ(単語/フレーズ/音声等)
  とは別の軽量なSQLiteファイルへ**無期限保持・直近2世代**でスナップ
  ショットするcronスクリプトを追加。
- `GET /api/system/admin/disk-usage`(新設) + 管理画面「その他」タブ:
  1GB予算に対する使用量(app.log・バックアップ)と、DB本体の内容別内訳
  (共有コンテンツ/ユーザーデータ/ログ履歴/その他)を表示。
- Caddyアクセスログ(`deploy/Caddyfile.study.snippet`)のローテーション
  を`10mb×5世代`→`100mb×40世代`に修正（100ユーザー規模のトラフィック
  試算で旧設定だと約13日分しか持たないと判明したため。約3ヶ月分・
  圧縮後ディスク使用量は約200MB見込み）。VPS側の実ファイル反映は
  別途`docker restart aipoc-web`で行う（他の相乗りサービスも数秒瞬断）。
- VPS crontabに2本追加（既存のcronは変更なし）:
  `30 3 * * * ... backup_user_data.py`、
  `45 3 * * * ... prune_usage_events.py`

## ver1.1.8 (2026-08-19)

**🐛 BASE OAuth連携の認可画面が404になる不具合を修正**: `base_oauth.py`の
`DEFAULT_SCOPE`がカンマ区切り(`"read_orders,read_users"`)になっていたが、
BASE公式ドキュメント(docs.thebase.in/api/oauth/authorize/)によると
scopeパラメータは**スペース区切り**が正しい仕様だった。実際に「BASEと
連携する」を実行したところBASE側の認可画面(admin.thebase.com)が404に
なる不具合が発生し、原因調査の結果この区切り文字の誤りが判明したため
`"read_orders read_users"`に修正。

## ver1.1.7 (2026-08-19)

**🆕 BASE注文の自動検知（Phase B）**

BASE APIのClient ID/Secretが発行され.envに設定済みだったため、従来
OAuth連携のみだった`app/routers/base_oauth.py`に続き、実際に注文を
検知して自動投入する部分(Phase B)を実装。

- `app/services/base_api.py`(新設): BASE API(`GET /1/orders`・
  `GET /1/orders/detail`)のクライアント。アクセストークンは1時間で
  失効するため、期限切れ間近なら`refresh_token`で自動更新（BASEの
  リフレッシュトークンは使用のたびローテーションするため都度DBへ
  保存し直す）。
- `app/routers/fulfillment.py`: `sync_orders_from_base()`を追加。
  直近注文を取得し、支払い済み(dispatch_status=ordered)かつ未登録の
  注文のみ`base_orders`へ自動投入（`base_order_id`のUNIQUE制約で
  冪等）。前回同期時刻を`app_state`に記録し、次回は2日分の重なりを
  持たせて取りこぼしを防ぐ。`POST /api/fulfillment/base-sync`
  エンドポイントを追加し、管理画面に「今すぐ同期」ボタンを設置。
- `scripts/sync_base_orders.py`(新設): 同じ同期処理をcronから無人実行
  するためのスクリプト。VPSのcrontabに30分おきの実行を追加
  （`*/30 * * * * ... eigo_base_order_sync`、既存のcron設定は変更なし）。
- ついでに`templates/admin_fulfillment.html`の日時表示がUTCのまま
  だったのをJSTに修正（他画面と同様の不具合）。

**現状の制約**: BASE側の認可(OAuth連携)は管理者本人がブラウザで
「BASEと連携する」を1回実行する必要があり、これはコード側では
代行できない。また新規注文の検知はキュー(base_orders)への自動投入
までで、メール/LINE等へのプッシュ通知は未実装（送信手段が未設定の
ため）。管理画面を定期的に確認するか、次のステップとして通知を
追加するかは別途検討。

## ver1.1.6 (2026-08-19)

**🆕 管理者情報画面を再構成（タブ分割）**

従来1本の長いページだった管理者情報画面を、
「👤ユーザー別管理」「📊ユーザー別使用状況」「📜ログ」
「📮問い合わせ対応」「🧾購入チャージキー対応」「🗂️その他」の
タブに分割。特に従来1つの表に混在していたユーザー一覧を、
操作(残高調整・強制ログアウト)用の表と閲覧専用の使用状況表に分離。

「ログ」タブは概要（未対応問い合わせ数・ロック中アカウント数等、
既存データから即表示）を先に出し、ログイン履歴・エラーログ・
アクセスログ集計・AI利用ログ検索・利用状況分析は折りたたみ表示に
した上で、開いたときに初めてAPIを取得するようにした（従来は画面を
開くたび無条件に5本のAPIを叩いていたのを解消）。期間・件数の指定は
既存のまま各項目内に残している。

## ver1.1.5 (2026-08-19)

**🆕 残高・設定の堅牢化（バックアップ）**

- **残高**: `add_balance()`(監査ログ`balance_ledger`に必ず1行残す)を
  経由しない残高変更経路が3箇所残っていた不具合を修正
  （AI利用の都度課金消費・音声再生の都度課金消費・管理者CLIの
  `setbalance`強制設定）。これで残高変更は理由(reason)・変更前後の値
  付きで**無期限に**全件追跡できるようになった（3世代限定より堅牢）。
- **設定(user_settings)**: 保存のたび変更前の内容を自動退避し、直近
  3世代を保持する仕組みを新設（`user_settings_backups`テーブル）。
  設定画面に「🛟 設定のバックアップ」カードを追加し、ユーザー自身が
  過去3件から選んで復元できる（復元前の内容も同じ仕組みで退避される
  ため、復元自体もやり直せる）。

## ver1.1.4 (2026-08-19)

**🐛 日別集計の「1日」の区切りをUTC日境界からJST日境界に修正**:
ver1.1.3では表示中の値をJSTに変換したが、「利用状況分析の日別推移」
「アクセスログ集計（日別）」は集計の**バケット境界自体**がまだUTC
(0時=JST9時)のままだった。
- `app/routers/system.py`の`admin_usage_analytics()`: `usage_events`の
  日別集計SQLを`datetime(created_at, '+9 hours')`で日付抽出するよう
  修正（created_atはSQLiteのUTC）。
- `scripts/analyze_access_log.py`（VPS cron・Caddyアクセスログ集計）:
  日付抽出をUTCから固定+9時間オフセット(JST、DST無しなので固定でよい)
  に変更。次回のcron実行時、まだCaddy側でローテーションされていない
  直近の日は自動的にJST境界で再集計される（既にログがロールオフして
  消えた古い日は遡って直せない・利用状況分析側はDBの生データが残って
  いるため全期間直る）。

## ver1.1.3 (2026-08-19)

**🐛 管理画面の各種ログがUTC表記のまま出ていたのをJSTに統一**:
サーバー(コンテナ)のシステム時刻・SQLiteの`datetime('now')`はUTCの
ため、ログイン履歴・AI利用ログ検索・お問い合わせ日時・残高変更履歴・
利用状況分析の最終アクセス・ユーザー一覧の最終学習・設定画面の
API使用量ログが、ラベル無しでUTCのまま表示されていた（実際の日本時間
より9時間遅れて見える不具合）。共通の`fmtDateJST()`
(`static/js/app.js`)を追加して全箇所をJST表示に変換し、該当する表の
見出しに「(JST)」を明記。エラーログ(`data/app.log`)も行頭タイムス
タンプのみJSTへ変換して表示。
※「アクセスログ集計（日別）」「利用状況分析の日別推移」の**日付の
区切り自体**（1日の境界）はUTC日境界のままで未対応（表示中の値が
JSTになっただけ）。直す場合はバックエンドのSQL/集計スクリプト側の
変更が必要なため別途対応。

## ver1.1.2 (2026-08-19)

**📝 「課金（チャージ後）」に収録数を追記**: 「このアプリについて」
ページの③課金セクションに、無料範囲を超えた単語・フレーズも少額消費
ですべて再生できることと、現在の収録数（単語12,000語以上・フレーズ
6,500件以上、実数は12,085語・6,699件）を追記。今後語彙が増えても
書き換え不要なよう「以上」表記にした。

## ver1.1.1 (2026-08-19)

**🐛 スマホ縦画面でトップバーのログインボタンが見切れる不具合を修正**:
`topbar-right`が折り返さず画面外にはみ出していたのを、狭い画面では
複数行に折り返すよう修正。あわせてテーマ(ダーク/ライト)によらず
ログイン/ログアウトへ確実にたどり着けるよう、ハンバーガーメニュー
(サイドバー)にもログイン/ログアウトを常設。

**🔍 SEO/LLMO向けにキーワード・メタ情報を拡充**: 表示は変更せず、
`<head>`のメタ情報(title/description/keywords/OGP/JSON-LD)と
AIクローラー向け`llms.txt`に、サブスク疲れ・前払い・廉価・無料体験・
専門用語/マイナー英語・AI活用による低価格・ブランド名(nyangailab/
にゃんがいらぼ)等のキーワードを織り込み。`llms.txt`には対象読者・
FAQ・BASEショップへのリンクも追加し、新規登録受付状況の古い記述の
食い違いも解消。

**📝 「このアプリについて」ページを再構成**: 内容量は変えずに、
概要→目次→登録するには？→課金して使うには？→ログイン前後/無課金・
課金の違い（詳細）→かんたん使い方ガイド→利用規約等→詳しい取扱
説明書→困ったときは、の順に再構成。目次を1箇所にまとめ各見出しへ
ジャンプできるようにし、各セクションから目次へ戻るリンクも追加。
「登録するには？」「課金して使うには？」を新設し、登録ページ
（`/login#signup`で新規登録フォームへ直接リンクできるようlogin.html
に対応）・BASEショップへの導線と、登録/課金するメリットを明記。

## ver1.1.0 (2026-08-18, commit `92623cf`)

BASEでの実購入を含む最終テスト（登録→注文記録→キー発行→償還→残高反映）を
本番で一気通貫で確認できたため、betaを外して正式版とする。

**🐛 登録画面のクライアント側ブロック解除漏れ**: サーバー側
(`SIGNUP_OPEN`)は再開していたが、`login.html`に残っていた独立の
「新規登録」クリック時ブロック処理により実際には登録できない不具合が
あった。修正し、本番で実際に新規登録が成功することを確認済み。

**🐛 マイク権限エラーの改善**: ハンズフリー会話等でマイク権限が拒否
された際、ブラウザの生の英語エラーがそのまま一瞬表示されるだけだった
のを、原因別（許可されていない/見つからない/使用中）の分かりやすい
案内に変更。取扱説明書に、Chrome/Edge/Firefox/Safari(PC/iOS/Android)・
Windows/Mac/Androidそれぞれのマイク許可確認手順を追記。

**🆕 ハンズフリー会話のつけっぱなし対策**: 発話の有無に関わらず一定時間
(既定15分)で強制終了する上限を新設。無音による自動終了も分単位で
設定可能にし既定を2分に変更（旧: 固定20秒）。「使い終わったら終了を
押すことをおすすめします」の注意書きを追加し、意図しない課金を防止。

**🐛 その他修正**: 設定画面のチャージカードをプロフィール直後に移動、
チャージキー入力欄へのブラウザ自動入力(ログインID)を防止、管理者画面
からフルフィルメント管理への導線を追加（アクセス制御は既存どおり
role=adminチェックで、URL直打ちでは非管理者は入れないことを本番で
再検証済み）、登録画面にBASE関連の案内（同一メール推奨・BASEへの
リンク・決済主体・商標表記）を追加。

## ver1.1.0-beta01 (2026-08-18, commit `dab78e1`)

**🆕 リリース前最終テストのため新規登録を再開**: `SIGNUP_OPEN`を`True`に
戻し、本人によるテストアカウント登録ができるようにした。signup/login/
チャージキー償還の成功・失敗を`ela`ロガー(`data/app.log`+`docker logs`)
に記録し、テスト中の挙動を追えるようにした。

**🆕 チャージキーの二重発行防止・発行状況の可視化**: 管理画面
(`/admin/fulfillment`)の「キー発行」に確認ダイアログを追加し、発行中は
ボタンを無効化して連打による二重発行を防止。同一注文への再発行時は
古いキーを`revoked_at`で失効させ、1注文につき償還可能なキーが常に
最大1本になるようにした(`charge_keys.revoke_key_by_id`)。発行済み/未使用/
使用済み/失効済みの件数と未使用分の合計金額を画面上部に表示
(`GET /api/fulfillment/stats`)。¥800/¥8,000(8,800pt)どちらの価格帯も
同一コードパスで動くことをテストスクリプトで確認済み。

**🆕 残高変更の監査ログ・管理者による手動是正**: 残高(balance_jpy)の
増減を`balance_ledger`テーブルに全件記録(理由・メモ・関連チャージキー・
操作した管理者を保持)。管理者情報画面のチャージ操作を、±¥10,000までの
増減両対応・理由入力必須に変更し(誤発行等の是正用)、対象ユーザーごとの
直近の変更履歴を画面から確認できるようにした
(`GET /api/system/admin/balance-ledger`)。

**🐛 お問い合わせフォームのメール必須化**: 返信先を確保できないケースを
防ぐため、メールアドレスをサーバー側・画面側の両方で必須化した。

**🐛 チャージキー入力欄へのブラウザ自動入力を防止**: 設定画面の
チャージキー入力欄にログインIDが自動入力されてしまう不具合を、
`autocomplete="one-time-code"`等への変更で修正。

## ver1.1.0-alpha43 (2026-08-18, commit `aaa9ef9`)

**🆕 習熟度(mastery)・忘却曲線のユーザー設定機能**: これまで固定値だった
「覚えた」判定の基準・満点・各ボタンの加点量・忘却曲線の強さを、
ユーザーごとに設定画面の詳細設定から調整できるようにした
(`app/services/spaced_repetition.py`の`MasteryConfig`)。既定値は
満点200pt・「覚えた」判定100pt以上・「覚えた」ボタン+200pt・
「うろ覚え」ボタン+25pt・忘却曲線は7日ごとに1pt減（従来の固定挙動と
同一）。「既定値に戻す」ボタンあり。忘却曲線はユーザーごとに独立した
基準日(`user_settings`内の`_decay_anchor`)で管理し、
`apply_forgetting_decay()`としてapp起動時に全ユーザー分をまとめて適用。
あわせて🔒「完全に覚えた」ボタン（満点固定・忘却曲線の対象外・解除可能、
`user_word_progress`/`user_phrase_progress`に`perfect`列を追加）と、
単語/フレーズ一覧の「クリア」ボタン（1pt以上の項目に表示・0ptへ完全
リセット）を新設。取扱説明書(`about.html`)に詳細を記載。バックエンド
テストスクリプトで全経路（既定値/カスタム値/不正値のクランプ/忘却曲線の
per-user適用/perfect除外）を確認済み。

**🧹 デッキ内クイズ機能の撤去**: `GET /api/decks/{id}/quiz`・
`POST /api/decks/{id}/attempt`(フレーズ帳版含む)が、フラッシュ単語/
フレーズ画面が`deck_id`で直接絞り込めるようになったこと
(下記フラッシュ画面機能追加)で完全に不要になっていたため撤去した
（ユーザー指摘で発覚。フロントエンドが一度も呼んでいなかった）。
連動して単語帳/フレーズ帳の「出題方向」「N回正解で習得」「忘却曲線を
使う」「1回の出題数」の設定項目（同様に未使用だった）も削除し、
デッキごとではなくアカウント共通の詳細設定(上記)に一本化した。

**🆕 フラッシュ単語/フレーズに単語帳/フレーズ帳選択を追加**:
「作った単語帳のフラッシュをやることが多い」との要望で、分野・レベルの
代わりに自分の単語帳/フレーズ帳を選んでフラッシュカードを開始できる
ようにした。バックエンドは`/api/words/quiz`・`/api/phrases/quiz`に
`deck_id`パラメータを追加（既存の分野/レベル条件と併用可・自分の
デッキであることを検証）。

**🆕 単語帳/フレーズ帳の一括追加UIの改善**: 分野・レベル(シーン・レベル)の
チェックボックス一覧すべてに「すべてチェック」「選択済み全クリア」を
追加。デッキ新規作成の「件数」欄に「全件」チェックボックスを追加し
（バックエンドは元々`limit=None`で全件対応済みだったがUIから選べな
かった）、該当する全項目を件数無視で追加できるようにした。

**📚 語彙拡充**: 「宗教」を世界の宗教の一般教養語彙(+43語、原罪・十戒・
カトリック/プロテスタント等)として拡充しつつ、日本固有の宗教
(神道・日本の仏教・山岳信仰・八百万の神・アイヌ/琉球の地域信仰・神事)
を新設の「宗教（日本）」ドメインに分離(taxonomy.pyに登録・計+99語+
フレーズ20件)。地理ドメインを気候・地形・地図中心に+83語(21→104語)。
武道・格闘技(ボクシング/柔道/剣道/忍術/合気道/古武術)+40語、日本文化
(茶道・華道)+16語、日本刀・火縄銃+11語。あわせて「スポーツ」を
「趣味・エンタメ」から独立した大分類に分割(taxonomy.py、単語・
フレーズ双方)。新規語は全てdetail(意味・例文等)・音声とも生成済み。

**🔒 OpenAI商標表記の追加**: `terms.html`の免責事項・`about.html`の
取扱説明書に、AIエンジンとしてOpenAIのモデルを使用している旨と、
「OpenAI」がOpenAI, L.L.C.またはその関連会社の商標である旨を明記
（既存のTOEIC/ETS商標表記と同じ形式）。

**その他**: 管理画面「利用状況分析」に管理者IPバッジ・AIクローラー/
検索bot件数のサマリーを追加。`about.html`の重複していたサンプル
閲覧セクションを削除（各機能画面で同等の体験がすでに提供されていた
ため）。

## ver1.1.0-alpha42 (2026-08-17, commit `81e5fa9`)

**🔴 重要バグ修正（alpha41の続報・fableモデルによる独立監査(Wチェック)で発見）**:
alpha41での禁止用語漏洩修正後、別モデル(fable)による独立した監査を
実施したところ、`GET /api/learn/assess`（AI学習診断）が学習履歴を
禁止用語チェックなしでAIプロンプトに渡していることが判明した。
`app/routers/learn.py`の`assess()`のSQLクエリに`banned_filter`を追加。

さらに、この経路の入口となっていた`/attempt`系エンドポイント
（`app/routers/vocabulary.py`・`phrases.py`の`/attempt`、
`decks.py`・`phrase_decks.py`の`{deck_id}/attempt`）は、任意の
単語/フレーズIDへの学習記録の書き込み自体に禁止用語チェックが無く、
悪意ある入力で禁止用語の学習履歴を作成→`assess`のAI診断文経由で
英文・和訳を間接的に引き出せる状態だった。4エンドポイントすべてに
書き込み時点のガードを追加（多層防御）。

あわせてfableによるフロントエンド(`static/js/`・`templates/`)の
独立監査も実施し、新たな漏洩経路は発見されなかった（チェックボックス
状態は常にサーバーへ送信される設計で、クライアント側だけの隠蔽は
存在しないこと、静的ファイル配信もDB/音声キャッシュを直接公開して
いないことを確認）。

なお、`GET /api/words/{id}/tags`がbanned IDでも404にせずタグ一覧を
返す点は、単語本文(英文/和訳)を一切含まないため実害なしと判断し
今回は見送り。また`/api/system/memory`が全ユーザー共有である点は
禁止用語とは別軸の問題として別途要検討。

## ver1.1.0-alpha41 (2026-08-17, commit `e6fb900`)

**🔴 重要バグ修正（禁止用語(差別用語等)が非管理者に漏洩しうる複数経路）**:
単語一覧で「範囲外」レベルを選択すると、禁止用語表示チェックを入れて
いなくても禁止用語が表示されるバグをユーザーから報告された。原因は
`app/routers/vocabulary.py`の`_word_filter`・`app/routers/phrases.py`の
`_phrase_filter`/`list_phrases`の禁止用語除外条件が
`(禁止用語でない OR レベル=範囲外)`というOR条件になっていたこと
（実データ確認: 禁止用語90語は全件レベル='範囲外'だったため、範囲外
チェック一つで全件漏洩する状態だった）。

この報告を機に`current_user_allow_banned()`によるサーバー側強制が
漏れている経路を横断調査し、以下も同時に修正した:
- `app/services/context_builder.py`の`review_words_today()`が
  `pick_weighted()`を`exclude_banned`未指定で呼んでおり、reading生成・
  英会話・trip-prep・ライティング添削・session summary等ほぼ全AI機能の
  プロンプトに禁止用語が混入しうる状態だった。
- `word_detail`/`phrase_detail`（単語・フレーズ詳細AI生成）がID直指定
  で一覧フィルタを迂回し、禁止用語の意味・例文を生成/返却していた。
- `tts_item`（リスニング再生）も同様にID直指定でフィルタを迂回し、
  禁止用語を音声合成・再生できていた。
- `decks.py`/`phrase_decks.py`の`_select_word_ids`/`_select_phrase_ids`
  が`word_ids`/`phrase_ids`明示指定時に`include_banned`判定を完全に
  スキップしていた（単語帳/フレーズ帳への直接追加で迂回可能）。あわせて
  `deck_words_list`/`deck_quiz`/`deck_phrases_list`/`deck_quiz`
  （フレーズ帳側）にも読み出し側の二重チェックを追加。
- `resolve_words`（類義語ジャンプ用API）がdomainチェックなしで禁止用語
  の意味・例文を直接返していた。

フラッシュ単語・フラッシュフレーズ(`/api/words/quiz`・`/api/phrases/quiz`)
は元々`_word_filter`/`_phrase_filter`を正しく使っており、今回の修正で
選択肢からも確実に除外される。全ファイルのimport確認・実データベースに
対するSQLクエリ整合性確認・既存デッキに禁止用語が混入していないことを
確認済み（本番デプロイは未実施）。

## ver1.1.0-alpha40 (2026-08-17, commit `52acdc7`)

**🟢 管理画面に利用状況分析を追加**: ユーザー要望により、画面別アクセス数・
機能別の音声再生回数・ボタン押下数・IPアドレス別のアクセス状況を管理画面
（👑管理者情報）で確認できるようにした。新設の`usage_events`テーブルに
3種類のイベントを記録する: 画面表示(page、`static/js/app.js`の`go()`)・
ボタン押下(click、`document`全体への委譲クリックリスナーで自動収集、
個々のボタンへの実装は不要)・音声再生(play、`app/routers/learn.py`の
`/api/learn/tts` `/tts/item` `/samples/{id}/tts`がキャッシュ済み/新規合成
いずれの再生時にもサーバー側で記録)。管理画面の新カード「📊 利用状況分析」
は`GET /api/system/admin/usage-analytics`をその場で集計して表示するため、
「更新」ボタンを押すと常に最新の内容が出る（事前集計・キャッシュ無し）。
画面別・機能別再生・ボタン押下(上位50)・IPアドレス別(ユーザー名付き)・
日別推移の5つの切り口で表示。ローカルでcurl実機確認済み（page/click/play
それぞれの記録・管理APIでの集計結果を確認、テストデータは削除済み）。
**本番デプロイは未実施**（次回デプロイ時の運用手順に従うこと）。

**🟢 誤連打・二重課金対策**: ユーザー要望により、無料範囲外の単語/
フレーズ再生で一度課金されたら、**同一コンテンツ（item_type+item_id+
kind別）は直近5分以内の再生では追加課金しない**ようにした
（`app/services/ai.py`の`charge_playback_if_needed`にプロセス内メモリの
5分TTLキャッシュを追加）。

なお、リーディング/リスニング教材・英会話・添削結果などAI生成コンテンツ
の読み上げ音声（`POST /api/learn/tts`）は、同一テキスト・同一声であれば
既存のディスクキャッシュにより**初回のみ課金・以降は期限なく無料**
（実測: 初回2.93秒＝実API呼び出し、2回目0.02秒＝キャッシュヒット）
だったため、5分ルールを新たに実装する必要はなかった。about.htmlの
取扱説明書にこの2つの挙動を追記した。

**🔴 設計方針の確認**: `free_range`によるAI利用ガード免除は、単語/
フレーズの無料範囲・公開サンプル教材の2箇所のみで使われており、
`chat()`/`chat_stream()`/`transcribe()`等の実際のAI生成呼び出しは
全て無条件にユーザー別無料枠・残高ガードを通ることを確認した
（バイパス経路なし）。

## ver1.1.0-alpha38 (2026-08-13, commit `ae93003`)

**🔴 重要バグ修正（無料範囲の音声で誤課金が起きうる抜け穴）**:
`synthesize_speech()`内の`_maybe_deduct_balance()`呼び出しが
`free_range`引数を考慮しておらず、単語/フレーズの無料範囲・公開
サンプル教材のいずれも、**未キャッシュの初回合成時に限り**、無料枠
¥0のユーザー(ゲスト/自己サインアップ)の実際のチャージ残高から
課金されてしまう抜け穴があった（`free_range=True`はAI利用ガードの
免除だけを見ており、その後の残高控除処理は見ていなかったため）。
`free_range=True`のときは残高控除自体をスキップするよう修正。

影響範囲の確認: 本番`ai_usage`を集計した結果、今回この経路を通った
のはゲスト疑似ユーザーと管理者アカウントのみで、いずれも
`balance_jpy`が未設定(None)のため実際の控除は発生していなかった
（実害なし）。ただし残高を持つ実ユーザーが同じ操作をしていた場合は
実際に誤課金されていた可能性があり、再発防止のため修正した。

## ver1.1.0-alpha37 (2026-08-13, commit `fb9a2b8`)

**🟢 UX改善（サンプル教材の音声再生）**: 未キャッシュのサンプル教材は
初回再生時にAI音声合成が走り数秒〜十数秒かかることがあり、その間
何の表示も無いため「止まっている/鳴らない」ように見えていた
（ユーザー指摘: ITサンプル等ですぐ再生されない）。再生ボタン押下時に
「⏳ 生成中…」表示＋ボタン無効化を追加し、待ち時間を可視化した。
あわせて、本番の公開サンプル教材全40件×男声/女声(計80件)の音声を
事前生成してキャッシュをウォームアップ済み（以後は全サンプルで
初回から即再生される）。

## ver1.1.0-alpha36 (2026-08-13, commit `6b7733d`)

**🔴 バグ修正**: alpha35で追加した`/sitemap.xml`・`/llms.txt`が、本番
(MULTIUSER=1)では未ログインアクセス時に`_AUTH_ALLOW`未登録のため
`/login`へ307リダイレクトされ、検索エンジン/AIクローラーから読めない
状態になっていた（ローカル開発はMULTIUSER=0のため気づけなかった）。
`app/main.py`の`_AUTH_ALLOW`に両パスを追加して修正。

## ver1.1.0-alpha35 (2026-08-13, commit `050a557`)

**🟢 SEO/LLMO強化**: 「サブスク疲れ」「サブスクなし」「無料使用可能」
「安い英語アプリ」等のキーワードでの発見性向上と、生成AI(ChatGPT/
Claude/Perplexity等)からの参照性向上を目的に対応。
- `GET /sitemap.xml` を新設（robots.txtのAllow対象と一致させた公開
  ページのみ列挙）。robots.txtに`Sitemap:`行を追記。
- `GET /llms.txt` を新設（llmstxt.org提案の慣習に準拠。サービス概要・
  公開ページ一覧・現在新規登録停止中である旨をAIクローラー向けに明記）。
- `static/about.html`: 「サブスクなし・無料使用可能」「安い英語アプリ」
  を明記する段落を追加。あわせて、新規登録案内の文言が
  「試験公開・招待制のためチャージキー入力が必要」のまま古くなって
  いた（alpha32で新規登録自体を停止したため事実と不一致だった）のを
  「新規登録を一時停止中・正式公開2026年9月予定」に修正。
- 既存のtitle/meta description/OGP/JSON-LD(SoftwareApplication)は
  既に「サブスク不要」等をカバーしていたため変更なし。

## ver1.1.0-alpha34 (2026-08-13, commit `0626c11`)

**🔴 バグ修正（Reading/Listening/英会話/Writingの音声がしわがれ声になる）**:
2026-08-12のゲスト/無課金ユーザー無料枠¥0化の副作用で、これら4機能の
音声再生(`POST /api/learn/tts`)が常時422で拒否され、フロントが無条件で
ブラウザ標準音声(しわがれ声)へフォールバックしていた（単語/フレーズの
🔊は alpha12 で同種の不具合を修正済みだったが、この4機能には未適用
だった）。
- 新設: `GET /api/learn/samples/{id}/tts` — 公開サンプル教材
  (`is_public_sample=1`)専用の読み上げ。単語/フレーズの「無料範囲」と
  同じ設計思想で、本文をIDからサーバー側で引く(任意テキストの無料合成
  には使えない)。未ログイン・無課金・課金いずれのユーザーも無料
  （一度誰かが再生すればキャッシュされ以後は生成コストも無料）。
  男声(ash)/女声(nova)を選べ、速度(標準/ゆっくり/速い)も選べる
  （単語/フレーズと同じ声・同じ操作感に統一）。
- AI がその場で追加生成する教材・会話・添削（サンプルでない動的な
  コンテンツ）は引き続きログイン必須・費用発生のまま（従来どおり）。
- フロント(`static/js/speech.js`)の`say()`/`sayWithVoice()`/
  `speakAndWait()`を、単語/フレーズの`sayItem()`と同じ方針に統一:
  サーバーが意図的に拒否(402/422等)した場合はブラウザ音声へ
  フォールバックせず、案内メッセージを出すだけにした
  （「変な声で鳴るくらいなら無音の方がよい」という既存方針を徹底）。

**🟢 管理者画面の拡充**:
- 「📈 アクセスログ集計」カードを追加。VPSのcronが日次生成する
  `data/analytics/access_summary.jsonl`（Caddyアクセスログの集計・
  AIクローラー/検索bot判定込み）を表示する
  (`GET /api/system/admin/access-log-summary`)。
- 「🔍 AI利用ログ検索」カードを追加。ユーザーID・期間・機能・モデル・
  IPで`ai_usage`を絞り込み検索できる
  (`GET /api/system/admin/ai-usage-search`)。テスト/開発起因の利用と
  実利用の切り分けに使う。

**🟢 UI微修正**: トップページのバージョン表記を、共用の`--muted`
(灰色・opacity .7)から`--accent`(青系)へ変更しフォントサイズを
10px→11pxに拡大。ダークモードで見づらいとの指摘のため
（ダーク=明るい水色、ライト=やや暗めの青。既存の`--accent`変数の
明暗設計がそのまま合致したため新規変数は追加していない）。

## ver1.1.0-alpha33 (2026-08-13, commit `d4d7ac4`)

**🔴 デプロイ事故の再発防止＋UI微修正**:
1. **rsync --delete事故の再発防止**: alpha32の本番反映作業で、コード
   同期用の`rsync --delete`がVPS専用の`deploy/.env.study`（gitignore
   対象・ローカルに存在しない秘密ファイル）を誤って削除する事故が発生
   （稼働中コンテナから復元し復旧済み・本番影響なし）。再発防止として:
   - `.gitignore`に`deploy/.env.study`を明示追加（今後ローカルに作成
     しても誤コミットされないように）。
   - `deploy/sync_code.sh`を新設。同期対象を`app/`/`static/`/
     `templates/`等**明示列挙(allowlist)**する方式にし、`deploy/`
     ディレクトリ自体を同期対象から外すことで`.env.study`に構造的に
     触れられないようにした。接続先は`docs/deploy_target.local.sh`
     （gitignore対象）に分離し、テンプレートは`deploy/deploy_target.example.sh`。
   - 本番シークレット（SSH鍵・`.env.study`）をprivateリポジトリ
     `business_plan`に追記のみの形でバックアップ（VPS/作業機いずれかの
     消失に備える）。詳細はCLAUDE.md「🔗連携している private リポジトリ」
     参照。
2. **ログイン画面のパスワード表示切替アイコンを変更**: 絵文字(👁/🙈)が
   環境によって実写風に表示され「怖い」との声があったため、一般的な
   線画アイコン（目・目に斜線のSVG）に置き換えた。

## ver1.1.0-alpha32 (2026-08-13, commit `4b01d09`)

**🔴 新規登録を一時停止（試験公開中のため）**: まだ試験公開中につき、
自己サインアップ（メアド+パス）の新規登録受付を一時的に停止した。
正式公開は2026年9月中を予定。
- サーバー側: `POST /api/auth/signup` の先頭で `SIGNUP_OPEN = False`
  （`app/routers/auth_routes.py`）をチェックし、403+エラーメッセージを
  返すようにした。再開時はこの値を`True`に戻すだけでよい。
- クライアント側: ログイン画面（`static/login.html`）の「新規登録」
  リンクをクリックしても新規登録フォームには遷移させず、ポップアップ
  （`alert`）で停止中である旨を表示するようにした。
- 既存ID保有ユーザーのログイン（`/api/auth/login`）には一切影響しない。

## ver1.1.0-alpha31 (2026-08-13, commit `98b2086`)

**🟢 語彙拡充（語彙拡張ペンディング中の例外実装、ユーザー指示による3件）**:

1. **地域別文化ドメイン新設**: 「芸術・文化」配下に日本文化/中国文化/
   アジア文化/欧州文化/米国文化/文化(その他の国)の6ドメインを追加。
   料理・祭り・宗教・歴史とは重複しない、価値観・社会規範・慣習に
   関する単語119語・フレーズ117件を追加。
2. **「医療(病名)」ドメイン新設**: 既存の「医療(症状)」は症状語彙が
   中心で体系的な病名一覧が無かったため、風邪から癌・成人病・重大
   感染症まで純粋な病名だけを集めた新ドメインを追加(172語)。
3. **「災害(種類)」ドメイン新設**: 既存の「災害」は避難・救援等の
   対応語彙が中心で、地震・津波・台風等の現象そのものの名称が
   手薄だったため新設(24語)。地震/津波/台風/ハリケーン/サイクロン等
   は既に「地学」等の別ドメインに存在していたため、既存の複数分野
   タグ機能(`word_domain_tags`)で「災害(種類)」を追加タグ付け(24語)。
   `magnitude`のみ「天文」の等級(明るさ)と同綴り別義語のため、地震
   規模の意味で新規に別行追加。

追加した単語・フレーズは全件について詳細解説(発音・意味・例文・
派生語・類義語等)と音声(ash/nova 2声)を生成済み(detail/音声とも
カバレッジ100%)。

## ver1.1.0-alpha30 (2026-08-13, commit `2e85b2a`)

**🔴 バグ修正（トップバーのログインボタンの文字が見えない）**: alpha29で
追加したグローバルリンク色指定により、`a:visited`(訪問済みリンク)の
詳細度が`.btn`クラスより高くなり、ボタン風リンク（トップバーの
「ログイン」ボタン等）の文字色が背景と同じ青になって見えなくなって
いた。`.btn`要素をリンク色指定から除外して修正。

**🟢 機能追加（フラッシュ単語/フラッシュフレーズのフィルターを一覧画面と
同等にスマート化）**: フラッシュカード開始画面の分野/シーン選択を、
単一selectから英単語/フレーズ一覧画面と同じ「大分類→分野(複数選択可)」
のカスケードUIに変更（共通コンポーネント`initCheckDropdown`を流用）。
バックエンドの`/api/words/quiz`・`/api/phrases/quiz`は元々カンマ区切り
での複数分野/シーン指定に対応済みだったため、フロント側の変更のみで
実現。

**🟡 UX改善**: 設定画面の「英単語の分野」「フレーズのシーン」セクション
全体も折りたためるように変更（個別の分野グループだけでなく大枠も
`<details>`化、同じ矢印表記で統一）。

## ver1.1.0-alpha29 (2026-08-13, commit `51e18d2`)

**🟢 機能追加・UX改善（ゲスト体験の充実・実機フィードバック多数）**:
- ダッシュボードもゲスト(未ログイン)に開放。単語帳・フレーズ帳のように
  ゲストが使えない機能は「🔒 ログインすると使えます」と明示するように
  変更（`/api/system/progress`をゲスト読み取り許可に追加）。
- サンプル閲覧をポップアップ表示から、課金ユーザーの生成結果と同様に
  画面下のカード内に直接表示する方式に変更。あわせて🔊読み上げ
  ボタンを追加（既存の`readAloudBar`を流用、英語部分のみ読み上げ）。
- reading/listening/writing/conversation_sampleの各10件中3件ずつが
  同じラベル(「日常会話」等)で重複表示されていたのを、内容に応じた
  個別ラベルに修正。
- ゲストには生成・送信・ハンズフリー開始等のAI呼び出しボタンを
  🔒表示にして最初から操作不可にする（従来はクリックできて401エラーで
  紛らわしかった）。
- 設定画面の「表示する分野・シーン」の折りたたみ(`<details>`)を、
  既定で全展開→折りたたみに変更。折り畳み状態でも開閉可能なことが
  分かるよう矢印アイコン(▶/▼)を追加。
- チャージキー入力欄に`autocomplete="off"`を追加。ブラウザがログインIDを
  誤って自動入力してしまう問題を修正。
- 「⚙️ 設定」タブを「⚙️ 設定・チャージ」に改名。

**🔴 データ修正**: B13(地域別英語: イギリス/オーストラリア/シンガポール/
インド英語)の4分野が`taxonomy.py`の分類対応表に未登録で、単語一覧・
設定画面で「その他」に紛れて見つけにくくなっていたのを修正（対応表に
明示的に追加、フォールバック自体は正常に機能していたためデータ欠落は
無し）。

## ver1.1.0-alpha28 (2026-08-13, commit `a731b18`)

**🔴 重大バグ修正（設定画面が非管理者に完全に開けなかった）**: 2026-08-12の
`GET /api/system/settings`管理者専用化のさい、`static/js/views.js`の
`settings()`がこのAPIをフォールバック無しで呼んでいたため、**管理者
以外の全ユーザーが設定画面自体を開けなくなっていた**（プロフィール・
チャージ・通知設定・セキュリティ(全端末ログアウト)等、管理者専用ではない
機能も巻き添えで使えない状態）。403時に`/api/system/my-usage`から
安全なフィールドのみでフォールバックするよう修正、管理者専用カードは
従来通り非表示のまま。

**🟢 機能追加（ゲストにreading/listening/writing/英会話タブのサンプルを開放）**:
未ログインでもリーディング・リスニング・ライティング添削・英会話の
各タブ自体をサイドバーから開けるように変更（従来は`about.html`の専用
ページ経由でしかサンプルを見られなかった）。`/api/categories`・
`/api/listening`をゲスト読み取り許可に追加。各画面の「生成」等の操作は
引き続き要ログインだが、未ログイン時にボタンが応答なく固まっていた
箇所を修正し、ログイン案内を表示するようにした。

**🟡 UX修正**: グローバルなリンク色を指定（`--accent`）。未指定だった
ため、ダークモードで訪問済みリンクがブラウザ既定の紫のまま読みにくく
なっていた。

## ver1.1.0-alpha27 (2026-08-13, commit `ffa7afe`)

**🟢 機能追加（管理画面にログイン履歴・エラーログを追加）**: 管理者情報
画面に「🔑 ログイン履歴（直近100件・成功/失敗）」「🐛 エラーログ
（data/app.logの末尾200行）」カードを追加。新規`login_log`テーブル
（成功/失敗とも記録）と`GET /api/system/admin/login-log`・
`GET /api/system/admin/error-log`（いずれも管理者専用）を新設。

**🟢 機能追加（reading/listening教材ストック・再利用）**: `POST
/api/learn/generate`で、reading/listeningかつ`force_new`未指定の場合、
自分がまだ学習していない（`user_material_progress`に記録が無い）同じ
分野の既存教材があれば、AI呼び出しをせずそれを再利用して返すように
変更（コスト削減・即時表示）。**他ユーザーの生成物は一切対象にせず、
常に自分自身が過去に生成した教材のみ**を参照する（2026-08-12の
materials所有者分離修正の方針を厳守）。学習済みにした後は通常通り
新規にAI生成する。

**🟡 規約・表記の更新**:
- ダッシュボードの「TOEIC推定スコア」について、AIによる簡易目安であり
  公式スコアではない旨・TOEICがETSの登録商標である旨・関連団体への
  問い合わせを控えていただきたい旨を`terms.html`/`about.html`に追記。
- 特定商取引法ページ: 支払い方法にBASE側の仕様変更で変わりうる旨を
  追記（BASE公式ヘルプで現行8種の記載に齟齬がないことを確認済み）、
  商品引渡し時期を「1〜3営業日」→「5営業日以内を目安に」に変更、
  返品・キャンセル欄に無料範囲での事前確認（音声再生の確認含む）を
  促す文言を追加。

## ver1.1.0-alpha26 (2026-08-13, commit `d3c3ee7`)

**🟢 機能追加（単語帳/フレーズ帳への「分類ごと一括追加」＋個別追加）**:
既存デッキの編集画面から、これまでの「収録中の一覧から削除」に加えて
以下を追加。
- 🎯 分野・レベル（フレーズ帳はシーン・レベル）を選んで一括追加。
- 🔍 単語/フレーズを検索して個別に追加（3文字以上でインクリメンタル
  検索、追加済みのものは「追加済み」表示でボタン無効化）。
- 新規`POST /api/decks/{id}/words`・`POST /api/phrase-decks/{id}/phrases`
  エンドポイント（無料範囲は既存の作成時制限と揃え合計100件までに制限）。

**🟡 UX修正**: 🆓アイコンの拡大方法を修正（ボタンのfont-sizeを直接上げると
外枠(パディング込みのボタン全体)まで大きくなってしまっていたため、内側の
絵文字だけ`transform: scale(1.2)`で拡大するように変更）。

## ver1.1.0-alpha25 (2026-08-13, commit `62328dc`)

**🔴 バグ修正（再生アイコンの🔒表示）**: alpha22で「管理者は🔒を出さない」
対応をしたが、**管理者以外でも残高のあるログイン済みユーザーは無料範囲外
でも実際には再生できる**(pt消費されるだけ)ため、依然として🔒が紛らわしい
状態だった（実機フィードバックで発覚）。「再生できるのに🔒はおかしい・
再生できないなら🔒でよい」という指摘に沿い、`state.canPlayOutOfRange`
（管理者=常に可・ゲスト=常に不可・それ以外は残高>0で可）を新設して
実際の再生可否に基づき🔒/🔊を出し分けるよう修正（`app.js`の
`refreshCost()`・`views.js`の`voiceButtonsItem()`）。

**🟡 UX修正**: 🆓アイコンが🔒/🔊より小さく見えるとの指摘により、フォント
サイズを1.2倍に拡大（`static/css/style.css`の`.btn.free-icon`）。

## ver1.1.0-alpha24 (2026-08-13, commit `50a4693`)

**🟢 機能追加・UX修正（登録画面のアンケート拡充＋見た目改善）**:
- **チャージキー欄を登録画面から削除**（設定画面の「💳 チャージ」に一本化）。
- 職業を「職業分類」（学生/教師・教育関係/会社員/公務員/自営業/
  フリーランス/経営者・役員/専門職/主婦・主夫/定年退職/無職/その他の
  単一選択）→「職業詳細」（選んだ分類に応じたチェックボックス群＋自由
  記入欄）の2段階に拡充。
- 「興味のある分野」を新設（単語/フレーズの17大分類と同じ選択肢を複数
  選択可）。
- 「利用目的」「このアプリを何で知りましたか」を自由記入からチェック
  ボックス（複数選択可）＋自由記入に変更。
- 年代の選択肢を「10歳未満」〜「100歳代以上」まで拡張。
- アンケートを既定で展開表示に変更（折りたたみ操作不要）。
- ログイン/登録画面にもメインアプリと同じ文字サイズ調整・ダーク/ライト
  切替を追加（`localStorage`の`theme`/`fontSize`キーを共有し設定が
  引き継がれる）。ダークモードの文字コントラストも改善。
- `users`テーブルに`survey_occupation_category`/`survey_occupation_detail`/
  `survey_interest_areas`列を追加。`privacy.html`の開示内容も更新。

## ver1.1.0-alpha23 (2026-08-13, commit `991c95b`)

**🟢 機能追加・UX修正（ユーザー指摘）**:
- 新規登録時の**チャージキーを任意化**。キーが無くても②ログイン無課金
  ユーザーとして登録でき、後から設定画面の「💳 チャージ」でいつでも
  入力できるように変更（従来はキー必須で自己サインアップ自体ができな
  かった）。
- 登録フォームに**パスワード確認欄**を追加（クライアント側で不一致を
  検出）。
- ログイン/登録のパスワード欄に**表示・非表示トグルボタン**（👁/🙈）を
  追加。
- **パスワードポリシー**を明文化: 8〜32文字、半角英字・数字・記号のみ
  使用可、英字のみ/数字のみ/記号のみは不可で2種類以上の組み合わせを
  必須化（サーバー側`auth.password_policy_error()`＋クライアント側の
  事前チェックの二重実装）。
- トップバーのpt残高表示をクリックすると「チャージしますか？」の
  確認ダイアログを表示し、「はい」で設定画面（チャージ）へ遷移する
  ように変更。

## ver1.1.0-alpha22 (2026-08-13, commit `20f7d69`)

**🟡 UX修正（ユーザー実機フィードバック）**:
- 残高表示を「¥」から「pt」表記に変更（トップバー・ダッシュボードの
  「チャージ残高」）。実際の値は元々pt(チャージキー償還時のpt数)と
  同じもので、表示ラベルのみの変更（管理者向けの実コスト表示・
  管理画面の残高/チャージ操作は¥表記のまま維持）。
- 単語/フレーズ一覧の再生アイコンを改善: 管理者は動作確認用に元々
  課金対象外(`ai.charge_playback_if_needed`)のため、無料範囲外でも
  🔒を出さず通常の🔊にするよう変更（「再生できるのに鍵マークが出るのは
  紛らわしい」という指摘）。あわせて無料範囲の語・フレーズには🆓を
  表示し「無料で再生できる」ことを明示。about.htmlの取扱説明書に
  🆓/🔊/🔒アイコンの意味を追記。

## ver1.1.0-alpha21 (2026-08-13)

**🟢 機能追加（新規登録画面の拡充）**: ユーザー指示により、新規登録画面に
以下を追加。
- 必須項目: 氏名・フリガナ・メールアドレス（確認用の再入力欄を追加、
  一致しない場合はクライアント側で送信前に弾く）。
- 任意項目（「📋 アンケート」として折りたたみ表示）: ご職業・年代・
  性別・利用目的・当サービスを知ったきっかけ・自由記入欄。回答は
  サービス利用に必須ではなく、改善の参考にのみ利用。
- `users`テーブルに`full_name`/`furigana`/`survey_*`列を追加（冪等
  マイグレーション、既存ユーザーは空文字のまま）。`privacy.html`の
  「取得する情報」に新項目を開示。

## ver1.1.0-alpha20 (2026-08-13, commit `c04f993`)

**🟡 UX修正（ユーザー実機フィードバック）**:
- 文字サイズ設定「大」「特大」の拡大率を引き上げ（`大`: zoom 1.15→1.32、
  `特大`: zoom 1.3→1.63。現在の倍率に対しそれぞれ実寸+2pt/+4pt相当を
  加算した値）。
- ダークモード時、一覧表のヘッダー文字（Lv/分野/習熟度等）が薄すぎて
  読みにくいとの指摘により、ダークモード限定で本文と同じ明るさに変更
  （ライトモードは従来通り）。

## ver1.1.0-alpha19 (2026-08-12, commit `7626556`)

**🟢 機能追加（文字の大きさ設定）**: topbarに「文字: 小/中/大/特大」の
セレクトを追加（既定は中）。`zoom`で全体を一括拡大縮小する方式（既存CSSの
個別font-size書き換え不要）。未ログインのゲストでも使える設定にしたい
という要望のため、要ログインの設定画面ではなくtopbarに常設。ピンチズーム
自体は元々`user-scalable`を無効化していないため既に可能（viewport meta
タグを確認・変更不要）。

**🔴 バグ修正（about.htmlのバージョン/リリース日が表示されない）**:
`Dockerfile`が`CHANGELOG.md`をイメージにコピーしていなかったため、
`/api/system/my-usage`の`version_date`が本番で常にnullになっていた。
`COPY CHANGELOG.md .`を追加。

## ver1.1.0-alpha18 (2026-08-12, commit `4827d02`)

**🟢 機能追加（フラッシュ単語/フラッシュフレーズの無料範囲UI）**:
- 「🔊再生できるものだけ」チェックボックスをフラッシュ単語/フラッシュ
  フレーズのセットアップ画面に追加（英単語/ミニフレーズ一覧と同様）。
  `/api/words/quiz`・`/api/phrases/quiz`に`free_range_only`パラメータを
  追加。
- 無料範囲外の単語・フレーズは🔊再生ボタンが🔒表示になるよう変更
  （フラッシュカード・英単語一覧・ミニフレーズ一覧の全て）。`_word_dict`/
  `_phrase_dict`が`is_free_range`を返すようになった。

**🟡 UX修正（このアプリについて画面）**:
- 「ログイン画面へ」ボタンを「アプリを開く（トップ画面へ）→」に変更
  （ログインはトップ画面右上から可能なため）。
- 各所の「このアプリについて」リンクの`target="_blank"`を削除し、同じ
  タブで開くよう統一（別タブで見たい場合は右クリックで選択可）。
- バージョン・リリース日をCHANGELOG.mdの最新エントリから自動取得して
  表示するように変更（`/api/system/my-usage`に`version_date`を追加）。
  ページの作成日/最終更新日も記載。
- AI生成コンテンツ（単語・フレーズ・音声・音声認識）に関する注意書きを
  取説冒頭・利用規約（免責事項）に追加。低コスト提供の理由がAI活用に
  あることも明記。
- 2026年9月中の正式公開を目指して準備中である旨を追記。

## ver1.1.0-alpha17 (2026-08-12, commit `afeec0d`)

**🔴 UX/軽微なセキュリティ修正（未ログイン時の管理者/設定タブの一瞬表示）**:
`boot()`がnavを全件描画→非同期でロール判定→管理者/ゲスト非公開タブを
隠す、という順序だったため、未ログイン者でも一瞬「👑管理者情報」
「⚙️設定」等が見えてちらついていた。ロール判定(`refreshCost()`)を先に
済ませてからnavを組み立てる順序に変更し、非表示にすべきタブはそもそも
DOMに挿入しないよう修正（`static/js/app.js`の`boot()`）。バックエンド側は
従来どおり個別に要ログインを強制済みのため実害は無かったが、表示上も
見えないようにする。

## ver1.1.0-alpha16 (2026-08-12, commit `3aceb02`)

**🟡 UX修正（残量¥0アラートの廃止）**: ページを開く/再読み込みするたびに
「AI利用の残量が¥0になりました。管理者のチャージで継続できます。」という
ネイティブalert()が出て邪魔、との指摘。特にゲストは仕組み上常に残量¥0
（無料枠なし）のため毎回鳴っていた。alert()自体を廃止（`static/js/app.js`
の`refreshCost()`）。残量はトップバーの数値表示（¥0で赤色）で従来どおり
確認できる。

## ver1.1.0-alpha15 (2026-08-12, commit `53939c6`)

**🟡 UX修正（自動読み上げチェックボックスの分かりやすさ）**: フラッシュ単語/
フラッシュフレーズの「答え表示で自動読み上げ」チェックボックスが分かりにくい
という指摘。ラベルを太字化し「答え表示で自動的に音声を再生する」に変更、
「※ONにすると音量にご注意ください」を赤文字で追加。

## ver1.1.0-alpha14 (2026-08-12, commit `1a5607f`)

**🟡 UX修正（フラッシュカードの採点ボタン表示）**: 「⏸ 保留」ボタンが一時停止
マークに見えて分かりにくいという指摘。「⏭ 次へ（飛ばす）」に変更（挙動は
従来どおりskip）。進捗表示の「保留N」ラベルも「スキップN」に統一。

## ver1.1.0-alpha13 (2026-08-12, commit `822172a`)

**🟡 UX修正（フラッシュ単語/フラッシュフレーズの自動読み上げ）**: 「開始」を
押した直後に音声が自動再生され、電車内等で驚くという指摘。「答え表示で
自動読み上げ」チェックボックスの既定値をONからOFFに変更（`static/js/views.js`
の`flashcard()`/`flashPhrase()`）。既に明示的に選択したことがあるユーザーの
localStorage設定はそのまま優先される。

## ver1.1.0-alpha12 (2026-08-12, commit `df11a40`)

**🔴 バグ修正（ゲストのしわがれ声）**: 2026-08-12の無料枠バグ修正
（`4fec867`）で、ゲスト疑似ユーザーの日次AI無料枠を0円に固定した副作用として、
無料範囲内（レベル低い順で単語1,000語・フレーズ750件）の単語・フレーズでも
「未キャッシュ（誰も再生したことがない）」ものは音声合成が拒否され、
フロントエンドがブラウザ標準の合成音声（しわがれごえ）にフォールバックして
いた。特にフラッシュフレーズ/フラッシュ単語で顕著だった（未収録の語を
引きやすいため）。

- `app/services/ai.py`: `_guard()`に`skip_user_cap`、`synthesize_speech()`に
  `free_range`引数を追加。無料範囲内と判定済みの再生はユーザー別の日次/月次
  無料枠チェックだけを免除する（サイト全体の上限・レート制限は従来どおり
  有効）。
- `app/routers/learn.py`: `tts_item()`で無料範囲判定を行い、
  `synthesize_speech()`に渡すよう変更。
- `static/js/speech.js`: `sayItem`/`sayItemAndWait`が、サーバーから明示的な
  エラー応答(402/422問わず)を受け取った場合はブラウザ音声へフォールバック
  しないよう統一（従来は402のみ対象で422は素通りしていた）。ネットワーク
  到達不能などサーバー応答が得られない場合のみ、引き続きブラウザ音声に
  フォールバックする。

## ver1.1.0-alpha11 (2026-08-12, commit `f874b4f`)

料金体系の見直し、忘却曲線(SRS)まわりのバグ修正、無料サンプル教材の拡充と
ゲスト公開をまとめて実施。

- **💰 課金体系の見直し**: AI利用料のカテゴリ別倍率
  （会話2.5倍/リーディング教材1.5倍・読み上げ1.7倍/リスニング教材1.5倍・
  読み上げ1.7倍/ライティング2.0倍/その他2.0倍）と一律+50銭サーチャージ
  （TTS/STTのみ対象外）に整理。競合アプリ（enja AI Talk・ELSA Speak・
  Duolingo・スタディサプリ等）と比較のうえ、チャージパックを
  ¥800(800pt)/¥8,000(8,800pt)の2種類に確定。
- **🔴 バグ修正（収益漏れ）**: TTS/STT利用が残高から一切消費されて
  いなかった（無料枠を使い切っても無制限に使えてしまう状態）のを修正。
- **🔴 バグ修正（無料枠ループホール）**: 自己サインアップした課金
  ユーザーが、tier判定のロジック不備により¥150/日の無料枠を永続的に
  受け取れてしまっていたのを修正（email設定の有無で判定するよう変更）。
- **🔴 バグ修正（SRS）**: フラッシュカードUIで「覚えた」「うろ覚え」を
  選んでも復習間隔(review_level/next_review)が更新されず、同じ単語が
  ずっと出題対象に残り続けていたのを修正。
- **新機能**: 「覚えた」判定の語彙・フレーズを出題から完全除外できる
  詳細設定(hide_mastered)を追加（個別に「戻す」ボタンで復活可能）。
- **新機能**: リーディング/リスニング/ライティング添削/英会話の各機能に
  つき10件ずつ、計40件のサンプル教材を追加（日常会話・スポーツ・
  ニュース・教養・歴史・AI・IT等、幅広く不快感のない話題を選定）。
  ログイン済みユーザー向けにライティング/英会話画面にサンプル表示UIを
  追加したのに続き、`materials.is_public_sample`列と新設の読み取り専用
  API `GET /api/learn/samples` により、未ログインのゲストでも
  `static/about.html`からサンプルを閲覧できるようにした
  （`/api/learn/materials`は実データを含むためゲストには非公開のまま）。
- **表示の一本化**: トップバーの「今日/今月」2本バーを廃止し、残高を
  ¥数値1本で表示（残り¥100以下オレンジ・¥50以下赤）。
- **法務ページの整合性修正**: `templates/tokushoho.html`の返品・
  キャンセル条項を`terms.html`側の文言（サービス終了通知は「努める」が
  「保証しない」）と一致させ、無料枠上限到達時の案内文から古い
  「管理者チャージ(¥500)」表記（自己サービス化前の名残）を削除し、
  実際のチャージキー登録フローを案内するよう修正。

## ver1.1.0-alpha10 (2026-08-11, commit `c21e4c5`)

「ログインで聴ける」と「(ログイン後も)チャージが必要」を必ず区別する
よう修正（ユーザー指摘: 混同すると苦情の原因になる）。

- **🔴 バグ修正**: ゲスト(①)向けの「ログインすると聴けます」メッセージが、
  実際はログインしても②の無料範囲(単語2,000/フレーズ1,500)にも入って
  いない語まで一律で表示されていた。ログインしても結局チャージが必要な
  語なのに「ログインだけで無料になる」と誤解させる状態だったため、
  `charge_playback_if_needed()`で②の無料範囲内かどうかも判定し、
  範囲外なら「ログインだけでは聴けず、少額のチャージが必要」という
  別メッセージを返すよう分離。
- `static/js/speech.js`: サーバーからの案内文が空だった場合の既定文言を
  「ログインすると聴けます。」から状況を決めつけない中立文
  「この音声は今は再生できません。」に変更(ログイン済みユーザーの
  要チャージのケースを誤って「要ログイン」と案内しないため)。

## ver1.1.0-alpha09 (2026-08-11, commit `6dd13d5`)

alpha08で本番公開したゲスト①アクセスを、実際にブラウザで確認したユーザー
からの3件のフィードバックを受けて改善。

- **🔴 バグ修正**: 無料範囲外の単語/フレーズを🔊再生すると、ログイン案内
  メッセージの後にブラウザ標準音声(しわがれ声)で再生されてしまっていた。
  HTTP 402(意図的なブロック)のときはブラウザ音声へのフォールバックをせず
  メッセージ表示だけで止まるよう`static/js/speech.js`の`sayItem()`/
  `sayItemAndWait()`を修正。
- **新機能**: 単語一覧・フレーズ一覧に「🔊再生できるものだけ」フィルターを
  追加(`free_range_only`クエリパラメータ・`access_tiers.free_range_id_filter()`)。
  ゲストは無料範囲(単語1,000/フレーズ750)、ログイン無料ユーザーはその
  範囲(単語2,000/フレーズ1,500)に絞り込める。
- **SEO/LLMO**: トップページに視覚的に隠したh1(スクリーンリーダー/
  クローラーには読める標準的な`.sr-only`手法)を追加し、「サブスク不要」
  「ニッチな英単語」「登録・ログイン不要でも無料で一部機能が使える」旨を
  明記。

## ver1.1.0-alpha08 (2026-08-11, commit `ee19b81`)

未ログイン(①ゲスト)でも実際の学習画面を使える本実装。トップページを
案内ページ(about.html)へのリダイレクトから、通常のSPAそのものに変更
（「トップページですが通常の学習画面でよいのでは？」というユーザー指摘
を受けての方針転換）。

- **①ゲストの本実装**: `/`は未ログインでもリダイレクトせず学習画面を
  そのまま返す。単語/フレーズの一覧・検索・詳細閲覧・フラッシュカードは
  ①も常時無料、音声再生も無料範囲内(単語1,000語+フレーズ750件・②の半分)
  なら無料で聴ける。範囲外は「ログインすると聴けます」と案内。
- **🔴 実装中に発見したセキュリティ/コスト穴を3件修正**:
  (1) `POST /api/words/retag`（分野/レベルをAIで一括分類・DB書き換え）に
  管理者チェックが元々無く、未ログインの誰でもAI課金とデータ改変を
  起こせた → `_require_admin`を追加。
  (2) 単語/フレーズの「詳細」は閲覧は全tier無料の方針だが、**未生成の
  詳細を新規にAI生成する経路**にゲスト向けガードが無く、`regen=true`や
  未キャッシュIDの連打で際限なくAI課金が発生しうる状態だった → ゲストは
  新規生成不可(キャッシュ済みの閲覧のみ無料)に変更。
  (3) ゲスト許可パスを`/api/words`/`/api/phrases`のprefixマッチで実装した
  際、隣接する`POST /api/learn/tts`（フリーテキストをそのまま音声合成・
  無料範囲チェック無し）を誤って一緒に開けそうになった → 実際に使う
  `/api/learn/tts/item`(ID指定・無料範囲判定あり)だけをフルパスで個別許可。
- **SEO/LLMO**: `/`が直接SPAになったことで失われるtitle/meta description/
  OGタグ/JSON-LDを`static/index.html`の`<head>`に移設（`about.html`は
  取扱説明書ページとして存続）。
- **UI**: サイドバー折りたたみ時にアプリ名が分からなくなる問題に対応し
  トップバーへブランド表示を追加。メニューに「このアプリについて」を
  常設リンク化。

## ver1.1.0-alpha07 (2026-08-09, commit `994f769`)

BASEでの有料販売に向けた開設対応と、注文フルフィルメント基盤を追加。

- **フルフィルメント管理画面(Phase A)** (commit `bbac067`): BASE注文のチャージ
  キー配送を「見逃さない」ための管理用。`base_orders`テーブル・
  `/admin/fulfillment`(管理者専用)・注文追加/一覧/キー発行/配送済み/キャンセルAPI。
  未配送は古い順＋24時間超過アラート。発行キー平文はDB非保存(発行API応答で
  一度だけ返す)。
- **B2: プライバシーポリシー新設・利用規約/特商法ページ更新** (commit `4b61698`):
  `static/privacy.html`新設(OpenAIへのデータ送信の明記等)、`terms.html`に返金方針/
  規約変更/準拠法を追加、`tokushoho.html`の価格を¥500・¥5,000の2本立て＋BASE決済
  手段に更新。
- **単語帳/フレーズ帳UX刷新＋単語/フレーズの完全削除を管理者専用化** (commit `4ce8b2c`)。
- **単語/フレーズ/例文再生のチャージ課金＋無料範囲外の詳細表示制限** (commit `b9fc015`)。

※BASEショップ開設の決定事項・次段階(BASE API連携=Phase B等)は非公開の
`docs/TODO.md`「🏪 2026-08-09 BASEショップ開設セッション」を参照。

## ver1.1.0-alpha06 (2026-08-08, commit `672fc60`)

- **チャージキー償還の総当たり対策**: エラーメッセージを統一
  （「見つからない/使用済み/シークレット不一致」を単一メッセージ化し、
  攻撃対象の絞り込みを防止）。ユーザー単位(1時間10回)＋サインアップ
  経由のIP単位、両方にロックを追加。

## ver1.1.0-alpha05 (2026-08-08, commit `82fc4f5`)

- **「このアプリについて」を独立ページ化**: `static/about.html`新設。
  利用規約・特定商取引法・取扱説明書へのリンクを集約し、ログイン前
  （`/login`フッター）・ログイン後（topbarのバージョン表示をリンク化）の
  両方からアクセス可能にした。
- **免責事項を強化**: コンテンツがAI作成である旨、試験・資格・昇進試験の
  不合格やTOEICのスコア、仕事上・会話でのトラブル等を含むあらゆる損害に
  ついて責任を負わない旨を具体的に明記。

## ver1.1.0-alpha04 (2026-08-08, commit `c2d0718`)

- **セキュリティ修正（重大）**: 管理者専用のはずの`PUT /api/system/settings`
  （OpenAI APIキー/モデル変更）と`GET /api/system/usage`（全ユーザー分の
  AI利用履歴閲覧）に管理者チェックが無く、ログイン済みなら誰でも操作・
  閲覧できた不具合を修正。
- **データ整合性修正**: 会話/読/書/文学/リスニングの習熟度が
  `categories`/`listening_topics`テーブルへの直書きで**全ユーザー共有・
  混在**していた不具合を修正。`user_category_progress`/
  `user_listening_progress`を新設し per-user 化（既存の当該進捗値は
  複数ユーザー分が混ざっていたため引き継がずリセット）。
- **AI悪用防止**: 自由会話・自由作文等のシステムプロンプトに、性的/違法/
  自傷他害・内部情報開示・課金回避・英語学習外の汎用代行を拒否する指示
  (`_SAFETY_NOTE`)を追加。ロールプレイ経由の誘導にも耐性があることを
  実地確認。
- **新機能**: フレーズにも単語と同様の「詳細」機能を追加
  （`phrases.detail`、格言・慣用句・誤解を招く言い回し・マナー系は
  由来・歴史的背景・類似表現まで厚く、日常フレーズは軽く、とAIが
  自動で濃淡をつける設計）。
- **AIモデル切替**: 通常会話等を`gpt-5.4-mini`→`gpt-5.6-luna`へ（コスト
  65-73%減）。構造化JSON生成(単語/フレーズ詳細)の品質問題を避けるため
  `OPENAI_QUALITY_MODEL=gpt-5.4-mini`を明示固定。切替に伴い判明した
  `gpt-5.6-luna`の`temperature`非対応（既定値のみ）も修正
  (`_temperature_kwarg`)。
- **UI**: ログイン・新規登録画面から利用規約・特定商取引法ページへの
  リンクを追加（未記入のため「作成中」表示）。利用規約にAI判定結果の
  免責と禁止事項を明記。
- **調査**: `gpt-realtime-2.1-mini`等の音声対話統合モデルを実地検証。
  コスト差(約¥0.3/ターン)は許容範囲だが、導入にはWebSocketベースへの
  設計変更が必要でモデル差し替えより規模が大きく、AI英会話機能自体の
  実利用が0件のため優先度は「検証待ち」に位置づけ（詳細: `docs/TODO.md`）。
- **不正利用対策**: サブエージェントによる27パターンの不正利用分析を
  実施。特に重大だった「ユーザーごとのAI日次/月次無料枠が実質無制限
  だった」不具合を修正（legacy/charged tierは¥150/日、email
  tier(自己サインアップ・未課金)は¥0円に是正、月次上限フォールバックも
  新設）。チャージキーの`CHARGE_KEY_SECRET`が未設定だった問題も修正
  （新規発行・本番反映、既存の未償還キーが0件であることを確認済み）。
- **STTモデルもenv設定可能化**: `OPENAI_STT_MODEL`(既定`whisper-1`)を
  追加。構造化JSON出力なのに`quality_model`未使用だった`example`/
  `command`エンドポイントも統一。
- **バージョン管理運用を新設**: このCHANGELOG.mdと、`CLAUDE.md`への
  「バージョンアップ時はcommit+push必須」ルールを追加。

## ver1.1.0-alpha03 以前

個別のバージョン移行点は明確に記録されていない（この運用ルール自体が
2026-08-08に始まったため）。alpha01は commit `c7a42eb`
「マルチユーザー基盤 + per-user進捗 + サクラVPSデプロイ一式」。
それ以前の詳細な変更履歴は `git log` および `docs/TODO_OLD.md` を参照。
